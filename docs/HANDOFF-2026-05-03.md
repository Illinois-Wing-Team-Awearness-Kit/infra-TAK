# HANDOFF — 2026-05-03

## Status
Done — pending operator test on VPS

## What was done
- Implemented all v0.9.2-alpha features in one build (consolidated from original v0.9.2 + v0.9.3 plans)
- Committed and pushed to `dev` branch (latest: `2e0175a`)

## Features shipped (v0.9.2-alpha)

1. **Authentik Reputation Policy** — `_authentik_setup_reputation_policy()` migration, bound to `ldap-authentication-flow`. UI card on Authentik page. API: `/api/authentik/reputation/*`
2. **SSH Fail2ban Jail** — opt-in sshd jail with Guard Dog alerts. UI card on Fail2ban page. API: `/api/fail2ban/ssh/*`
3. **TAK Server Snapshots** — `_tak_snapshot()` captures CoreConfig, JVM heap, certs, pg_dump. Pre-upgrade snapshot blocks upgrade on failure. Scheduled via systemd timer. API: `/api/takserver/snapshot/*`
4. **TAK Server Rollback** — `_tak_rollback()` stops TAK, restores all files + pg_restore, restarts. UI Restore button. API: `/api/takserver/rollback`
5. **Console Rollback** — update_apply() records git tag before pull. Yellow rollback bar on Console page. API: `/api/console/rollback`

## Current state
Code complete, syntax checked, pushed to `dev`. NOT yet tested on a live VPS.

## Exact next step
Operator pulls `dev` on VPS and tests all five features. After validation, merge to `main` and tag `v0.9.2-alpha`.

```bash
cd ~/infra-TAK && bash pull-dev-and-restart.sh
```

## Files touched
- `app.py` — VERSION, new functions + API routes + template cards
- `docs/RELEASE-v0.9.2-alpha.md` — full release notes (all five features)
- `README.md` — latest release line updated to v0.9.2-alpha
- `docs/RELEASE-v0.9.3-alpha.md` — deleted (consolidated into v0.9.2)
