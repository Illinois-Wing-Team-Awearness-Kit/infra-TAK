# Tech Context — infra-TAK

> Technologies, dev setup, constraints, and dependencies. Update when stack changes.

## Stack at a glance

| Layer | Tech | Where |
|---|---|---|
| Language (console) | Python 3 (Flask + Gunicorn) | `app.py`, served on `:5001` HTTPS |
| Language (Node-RED subsystem) | Node.js (build script) + Node-RED runtime | `nodered/build-flows.js`, `nodered/flows.json` |
| OS (supported) | Ubuntu 22.04 LTS (production target) | Goal: universal installer |
| OS (dev) | macOS (Apple Silicon, this maintainer) | Code edited locally, deployed to VPS |
| Container runtime | Docker + Docker Compose v2 | All services except TAK Server itself |
| Reverse proxy | Caddy 2.x | Let's Encrypt + forward auth |
| Identity | Authentik 2026.x | LDAP outpost + embedded forward auth |
| TAK Server | Official `.deb` from tak.gov, 5.x | Native install (not containerized) |
| Database (TAK) | PostgreSQL 15 | Local or remote (two-server) |
| Database (Authentik) | PostgreSQL 16 (`postgres:16-alpine` in compose) | Container, separate DB |
| Process supervision | systemd | `takwerx-console.service`, Guard Dog timers |
| Monitoring | Guard Dog (custom bash + systemd timers) | TAK Server health, disk I/O, CoT DB size, certs |
| Auth: console | Session-based, password hash in `.config/auth.json` | Plus IP-mode backdoor at `:5001` |
| Auth: services behind FQDN | Authentik forward auth via Caddy | TAK Portal, Node-RED, etc. |
| Auth: TAK Server | LDAP via Authentik outpost (`:389`/`:636`) | Plus optional flat-file fallback |

## Console runtime

- **Entry point:** `app.py` (loaded by Gunicorn).
- **Port:** `5001` HTTPS (self-signed by default, switchable to LE via Caddy in FQDN mode).
- **Service unit:** `/etc/systemd/system/takwerx-console.service` — `User=root` in v0.9.x (non-root migration scheduled for **v1.0.0** as the major-version disruptive change; see `docs/PLAN-v1.0.0.md`. Scaffolding is in place via `_sudo_wrap`/`_write_priv`/`_read_priv`).
- **Working directory:** Whatever `start.sh` was run from on first boot (typically `/home/takwerx/infra-TAK`, but NOT guaranteed — varies per install). **ALWAYS resolve dynamically:** `grep -oP 'WorkingDirectory=\K.*' /etc/systemd/system/takwerx-console.service`. Never hardcode `/root/infra-TAK` or `/home/takwerx/infra-TAK` in pull commands given to the operator — this causes `cd: No such file or directory` failures.
- **Config / state:** `<install_dir>/.config/auth.json` (password hash, mode 600), `<install_dir>/.config/settings.json` (FQDN, `last_version`, migration outcomes, schedules).
- **Logs:** `journalctl -u takwerx-console`. UI long-running ops stream `plog` lines into the per-page log panel.

## Dependencies (Python)

The list lives in `start.sh` (apt + pip) — no `requirements.txt` is committed because deps are pinned in code. Notable:
- `Flask`, `gunicorn` — HTTP server.
- `requests`, `urllib.request` — outbound HTTP (Authentik API, Caddy admin, etc.).
- `cryptography` / `pyOpenSSL` — cert generation, parsing, validation.
- `paramiko` — SSH for remote deploys (Authentik / CloudTAK / MediaMTX / Node-RED / Federation Hub remote, two-server TAK).
- `bcrypt` / `werkzeug.security` — password hashing.
- `psycopg2-binary` — Postgres queries (TAK DB diagnostics, Authentik DB direct queries, snapshot pre-flight checks).

## Dependencies (Node-RED side)

- `nodered/node-red:4.0` (image — pinned, not `:latest`).
- Container runs as UID `1000:1000`, `cap_drop: [ALL]`, `no-new-privileges:true`, `mem_limit: 2g`, `restart: unless-stopped`.
- Contrib packages installed inside the container — pinned (no `^`/`~`) per `docs/NODERED-OPERATIONS.md`.
- `build-flows.js` runs both on host (during `nodered/deploy.sh`) and inside the container (post-install hook).

## External dependencies (per service)

- **Authentik** — Docker images from ghcr.io. Bootstrap creds auto-generated. PostgreSQL 16 + Redis (compose).
- **TAK Server** — Operator-uploaded `.deb` from tak.gov. PostgreSQL 15 (apt install or remote).
- **Caddy** — apt repo (cloudsmith). Let's Encrypt for ACME.
- **CloudTAK** — Docker image from upstream (compose).
- **MediaMTX** — Docker image from upstream (compose).
- **Node-RED** — `nodered/node-red:4.0` Docker image.
- **Federation Hub** — Operator-uploaded `.deb` from tak.gov (same packaging as TAK Server).
- **Guard Dog** — Pure bash + Python helpers; no external runtime deps beyond what TAK Server already requires.
- **Email Relay (Postfix)** — apt install, configured against operator-supplied SMTP relay (Mailgun, SendGrid, AWS SES, Gmail App Password, etc.).

## Network & ports

| Service | Port | Proto | Notes |
|---|---|---|---|
| infra-TAK Console | `5001` | HTTPS | Backdoor (always direct IP) |
| Caddy | `80` / `443` | HTTP/HTTPS | Reverse proxy + LE |
| TAK Server | `8089` | TLS | TAK client connections |
| TAK Server | `8443` | HTTPS | Admin (client cert auth) |
| TAK Server | `8446` | HTTPS | Admin (LE cert + LDAP/password auth) |
| TAK Server | `8087` | TCP | Disabled by default |
| PostgreSQL (TAK) | `5432` | TCP | localhost or remote (two-server) |
| Authentik | `9090` | HTTP | API + admin (proxied via Caddy) |
| Authentik | `9443` | HTTPS | Direct, rarely needed |
| LDAP outpost | `389` / `636` | TCP | TAK Server uses for auth |
| TAK Portal | `3000` | HTTP | Proxied via Caddy |
| Email Relay | `25` | SMTP | localhost only |
| Node-RED | `1880` | HTTP | Proxied via Caddy on FQDN, direct on IP-mode |
| MediaMTX | `8554` / `8889` / `5080` | RTSP / WebRTC+HLS / HTTP | Editor on `:5080` |
| CloudTAK | `5000` | HTTP | Proxied via Caddy |
| Federation Hub | `9100` (admin) / `9101–9103` (peers) / `8080` (Caddy → localhost) | HTTPS / TCP / HTTP | |

## Deployment topologies

1. **Single-server.** All services + console + TAK Server on one VPS. Default.
2. **Two-server TAK.** Server One = PostgreSQL DB. Server Two = TAK Server core + console + everything else. Configured per-host in TAK Server settings; deploy from console via SSH.
3. **Remote per-service.** Authentik / CloudTAK / MediaMTX / Node-RED / Federation Hub each can be deployed to a different host via SSH. Console SSHs and runs Docker/scripts there.
4. **External DB.** TAK Server can target AWS RDS, Azure Database for PostgreSQL, Google Cloud SQL, or any PostgreSQL 15 host (`docs/EXTERNAL-DB-SETUP.md`).

## Test/maintainer infrastructure

- **VPS — `tak-10` (`172.93.50.47`):** maintainer's primary test box. SSDNodes. Tracks `dev` branch. Repo at `/home/takwerx/infra-TAK` — **BUT NEVER HARDCODE THIS PATH**. Always resolve via `grep -oP 'WorkingDirectory=\K.*' /etc/systemd/system/takwerx-console.service`. Pull command: `cd $(grep -oP 'WorkingDirectory=\K.*' /etc/systemd/system/takwerx-console.service) && git fetch origin dev && git checkout -B dev origin/dev && sudo systemctl restart takwerx-console`. Configurator at `http://172.93.50.47:1880/configurator`.
- **VPS — `responder`:** secondary test box, busier (Mission API / DataSync load) — used for spiral-detection / load testing.
- **Azure / AWS test boxes:** spun up ad hoc for fresh-install validation, especially around NAT (`start.sh` shows public IP via `curl api.ipify.org`) and slow disks.
- Maintainer dev machine: macOS (this workspace is `/Users/andreasjohansson/GitHub/infra-TAK`). Code is edited locally, pushed to `dev`, pulled on VPS.

## Constraints

- **Ubuntu 22.04 LTS only** (current). Goal is universal installer.
- **Root required** for `start.sh` (writes systemd unit, installs packages, opens privileged ports). Console itself currently runs as root (v0.9.x) — non-root migration scaffolding exists but is gated.
- **Disk I/O matters.** SSD-backed storage strongly recommended; <200 MB/s sync write causes Docker build timeouts and Authentik startup failures. The README has a `dd if=/dev/zero of=/tmp/testfile bs=1M count=1024 oflag=dsync` pre-flight check for operators.
- **8 GB RAM minimum** for TAK Server, more for the full stack. 4 GB swap is auto-provisioned on Authentik deploy.
- **Container egress** is generally open by default; opt-in egress allowlist exists for Node-RED (`scripts/nodered-egress-firewall.sh`).
- **Image pulls require internet** at deploy time. Air-gapped install is not yet supported.
- **TAK Server `.deb` must be operator-supplied** (license + tak.gov signup is theirs to handle).

## Versioning

- Single `VERSION = "X.Y.Z-alpha"` constant near the top of `app.py` (currently `0.9.29-alpha` — **RELEASED 2026-05-18 to `main` after fleet validation across 4 boxes (test6 + test8 + test12 + tak-test-4 / Azure fresh deploy). Bundles the Project Shakespear 2026-04 security audit response (CRIT-08 SSH host-key pinning across 9 guarddog scripts + 2 raw-SSH callsites, CRIT-07 eval cleanup in 3 guarddog scripts, MED atakatak default-password nag, LOW CSP `unsafe-eval` drop, 388-line `SECURITY-AUDIT-RESPONSE-2026-04.md` per-finding response) WITH the Authentik MAX_REQUESTS cascade short-circuit (post-v0.9.28 stability fix discovered during soak on tak-10 2026-05-18: v0.9.23 autotuner's `+25%/30min` tune-UP cascade after a leak burst produces 10 server+worker recreates over 7 hours climbing from floor=100 to baseline=1000; new idempotent startup migration `_patch_authentik_max_requests_snap_to_baseline_if_pgbouncer` snaps straight to baseline in one recreate when PgBouncer is installed AND quiet for 6h AND no autotune in last 30min). Also ships new Cursor rule `.cursor/rules/no-main-merge-no-tag-without-permission.mdc` codifying the operator-permission gate established during v0.9.28 shipping. Previous v0.9.28-alpha — RELEASED 2026-05-17 to `main`, enterprise Authentik PG scaling for 12-core / 48 GB minimum hardware (production-scale TAK deployments with 100s of TAK clients per Authentik). Field-validated across 3 dev boxes (test6/test8/test12).** PG `max_connections` 500 → 2000 + memory tuned for 48 GB (`shared_buffers=12GB`, `effective_cache_size=36GB`, `work_mem=16MB`, `maintenance_work_mem=2GB`, `wal_buffers=64MB`, `max_wal_size=4GB`). Autotune ceiling 60% → 75% (effective 1500 conns, **5× v0.9.27 headroom**). Cold-start fleet safe-constant 250/50 → 750/150. Pool floor 75/15 → 300/60. PgBouncer `MAX_CLIENT_CONN` 1000 → 5000. New constant `_AUTHENTIK_PG_COMMAND_ENTERPRISE` is the single source of truth for the PG command line (referenced by install template + Update-Now migration paths — no more drift across 3 inline copies). Adds Channels-baseline telemetry (`peak_channels_idle`, `avg_channels_idle`, `samples_with_classes`) persisted to `settings.json → pool_autotune.last_decision` for forensic drift tracking across releases. Removes v0.9.27's operator-override carve-out in `_ensure_authentik_compose_patches` per `.cursor/rules/fleet-uniform-config.mdc` — every box converges to canonical enterprise command. Pool migration also reconciles `MAX_CLIENT_CONN` drift (field-discovered hotfix: pool-size-match short-circuited pgbouncer recreate when only `MAX_CLIENT_CONN` had drifted). Sized in response to Authentik upstream [#20714](https://github.com/goauthentik/authentik/issues/20714) (confirmed bug, `django_channels_postgres` pool leak, maintainer-endorsed mitigation is capacity bump until upstream 2026.8.0 ships in ~6 months) + Tom Endress's `anctakserver2` field forensic (78/85-conn pool saturation = 92% under moderate load). Test8 field result: `peak_channels_idle=85, avg=7.1` — directly mirrors Tom's observation, confirms v0.9.28 sizing math. Test6/test12 at autotune-floor 300/60 (correct for quiet load). All 18 Authentik containers healthy + zero `query_wait_timeout` across all 3 boxes. v0.9.27 self-healing layers (autotune + cl_waiting demand signal + continuous in-place remediation + ghost-Channels reaper) all preserved unchanged — just sized for enterprise hardware. **Previous v0.9.27-alpha — released 2026-05-17 to `main`, four-hotfix iteration on `dev` then squash-merge, field-validated across three test boxes (tak-10 + test8 + responder), 20-min clean soak with all 18 Authentik containers healthy and zero query_wait_timeouts.** Architectural fix for v0.9.26's silent fleet-fracture bug + four-layer self-healing pool management. v0.9.26 used `max(cur, target)` in `_ensure_authentik_pgbouncer_pool_size` to "preserve operator overrides" — which silently let tak-10 keep its operator-typed 250/50 pool while test8 (no override) got the codified 75/15 → test8 hit 297 `query_wait_timeout` events in 5 min, server-1 unhealthy. **The 65-min v0.9.26 validation on tak-10 was on a config the codebase never produced.** v0.9.27 ships in four hotfix iterations, each addressing a failure mode discovered live during dev validation: (1) **Initial autotune + hotfix #1 — cold-start fleet constant.** Replace v0.9.26's constant with a deterministic autotuner: `peak = max(idle_count over last 30 watchdog samples ≈ 1 h)`; `target_ceiling = max(FLOOR=90, ceil(peak × 2.0))`; capped at `PG_MAX_CONN=500 × 0.6 = 300`; split 5:1 default:reserve. Migration **always writes** autotune output — no `max(cur, target)`. Cold-start (samples < 3) uses a known-safe fleet constant 250/50 (replaces hotfix #0's unreliable single-shot live `pg_stat_activity` seed that under-sized tak-10 during a transient mid-restart lull). (2) **Hotfix #2 — cl_waiting demand telemetry + stuck-at-floor escape.** Idle-count alone measures pool *headroom* not *demand* (a starved box has all conns checked out → low idle → autotune confirms small pool is "fine," self-reinforcing under-sizing). New `_authentik_pgbouncer_cl_waiting()` queries `SHOW POOLS` for `cl_waiting + cl_waiting_cancel_req` — direct demand signal. Escape: if ≥ 3 samples with cl_waiting telemetry have any > 0, force fleet safe-constant. **Witnessed firing twice in production on test8.** (3) **Hotfix #3 — continuous in-place remediation.** Watchdog tick triggers `_ensure_authentik_pgbouncer_pool_size()` directly when 3 consecutive ticks show sustained starvation AND current_pool < 250 AND cooldown elapsed → pgbouncer recreate in ~30s, no operator action. **Detection-to-remediation: ~6.5 min worst case.** Triple-nested try/except so any failure here is swallowed and the original safety net (idle > threshold → restart server-1) always runs. (4) **Hotfix #4 — ghost-Channels-conn reaper.** test8 forensic revealed 290 `SELECT DISTINCT django_channels_postgres_groupchannel` backends frozen in `state='idle'` for 2h 2min with consecutive PIDs — leaked by prior server-1 restarts (Authentik's Channels long-poll listeners die mid-poll, PgBouncer can't see disconnect in transaction mode, ghost conns pile up). New `_authentik_reap_ghost_channels_conns()` runs narrow `pg_terminate_backend` SQL from BOTH startup migration (self-heals every v0.9.26 → v0.9.27 Update Now) AND watchdog tick on `cl_waiting > 0` or `channels_idle > pool_ceiling/2` (40-min cooldown). Manual test on test8 killed 290 ghosts, idle 299→11, all containers healthy in <60s. New cursor rule `.cursor/rules/fleet-uniform-config.mdc` codifies the fleet-uniform principle + the 3-box / ≥ 20-min soak validation gate. **Previous v0.9.26-alpha — released 2026-05-17 to `main`, four hotfix waves + one amendment, field-validated on tak-10 with 1 h 5 min continuous stability; restores v0.9.24-era "Authentik perfectly quiet" baseline.** Initial release: closed the long-running silent failure in `takauthentiktasklogpurge.service` (VACUUM-in-transaction bug + 30-day-only window) that let `authentik_tasks_*` grow to ~960 MB / 2.66M rows on tak-10 and tipped `authentik-server-1` into a sustained ~80% CPU CancelledError storm. **Hotfix #2 (13:55 UTC):** added `REINDEX TABLE CONCURRENTLY` + `VACUUM FULL` post-DELETE escalation + `_recreate_authentik_server_worker` to recover deeply-bloated installs where `VACUUM ANALYZE` couldn't shrink heap/indexes. **Hotfix #3 (15:30 UTC):** bumped PgBouncer `DEFAULT_POOL_SIZE` 35 → 75 and `RESERVE_POOL_SIZE` 5 → 15 after diagnosing that **Django Channels' PG-backed `groupchannel` long-polling SELECTs were eating ~17 of 35 pool slots**, triggering `ProtocolViolation: query_wait_timeout` → gunicorn workers stuck on `pg_advisory_lock(...)` → LDAP searches returning empty → TAK Server 8446 routing `webadmin` to WebTAK instead of admin. `_ensure_authentik_pgbouncer_pool_size` migration extended to handle both DEFAULT and RESERVE so all prior installs (25/5, 35/5) converge to 75/15 on next boot. **Hotfix #4 (16:30 UTC — Tom Endress's anchortak incident report):** new `_ensure_vm_overcommit_memory()` startup migration sets `vm.overcommit_memory = 1` on the host kernel (runtime + `/etc/sysctl.conf` persistence) so Redis BGSAVE forks don't fail with ENOMEM and trigger `stop-writes-on-bgsave-error`. ak-pg-watchdog enhanced to break down idle PG connections by query class (Channels / dramatiq / cache / advisory_lock / other) so operators can SEE which class is saturating the pool at a glance instead of running ad-hoc `pg_stat_activity` queries. Documents Tom's `2026-05-17 05:30 UTC` anchortak outage (4-cron coincidence + dramatiq `_fetch_pending_messages` SELECT FOR UPDATE without SKIP LOCKED → 36 thread lock-race → pool exhaustion) AND tak-10's parallel Channels-class failure (4 workers × 36 channels = 144 long-polling SELECTs) in a single forensic narrative. **Hotfix #4 amendment (16:43 UTC):** new `_reconcile_watchdog_threshold(default, reserve)` inside `_ensure_authentik_pgbouncer_pool_size` computes `channels_pool_watchdog_threshold = pool_ceiling + 50` and writes it to `settings.json` on BOTH the early-return path (operator override above codebase target) AND the bump-and-persist path. Closes the tak-10 false-restart-loop where the hardcoded 150-idle threshold misfired on the operator-overridden 180-conn pool ceiling.
- Sidebar shows the running version. Mismatch with the "Latest release" line in the README's top-of-file pointer indicates the customer is behind.
- Tags are pushed to GitHub when a release is shipped (`v0.9.4-alpha`). Pushing the tag is what triggers the in-product "Update Available" banner on customer installs (the customer's console polls GitHub releases).
- Update flow: customer clicks "Update Now" → console does `git fetch && git reset --hard origin/main` (or `dev` for testers) → restarts → `_run_post_update()` runs the migration ladder.
- Console rollback: every "Update Now" records `settings.console_rollback = {available, version, tag, snapshot_at}` so the operator can revert in one click for that update cycle.

### Release roadmap (post v0.9.11)

- **v0.9.29 — Security audit touch-up + 8446 webadmin-login root-cause fix (`ns0:` namespace prefix in CoreConfig.xml) + Authentik MAX_REQUESTS cascade short-circuit + MediaMTX Caddy-LE cert read-access + cloud public-IP detection + mediamtx-webeditor PermissionError fail-loop fix — RELEASED 2026-05-18 to `main`.** Final fleet-validation soak across 4 boxes (3 × SSDNodes prod: test6, test8, test12 + 1 × Azure fresh: tak-test-4) confirmed all on `da3f7ef`, `<ns0:` count = 0 in `/opt/tak/CoreConfig.xml` on every box, 8446 connectors all serving Let's Encrypt (issuer E7/E8, not the self-signed `cert_https`), MediaMTX + mediamtx-webeditor `active` on all boxes with ports `:5080` / `:8888` / `:9898` bound, 0 `PermissionError` errors in the last 5min on any box, all 5 subdomain vhosts (tak, takportal, nodered, stream, infratak) returning HTTP 302 to OAuth on all 4 boxes, every Authentik container healthy, `ak dump_config` confirms `AUTHENTIK_WEB__WORKERS=4` + `AUTHENTIK_WEB__MAX_REQUESTS=1000` are being loaded (per `.cursor/rules/consult-upstream-docs.mdc` — the v0.8.2 → v0.8.6 silent-ignore regression class stays closed). **Final fix (added during fleet soak, post-`0e48417`):** field-discovered `mediamtx-webeditor.service` `PermissionError` fail-loop on `tak-test-4` (264 restarts in ~1h before discovery). The upstream `mediamtx_config_editor.py` from `takwerx/mediamtx-installer` hard-codes `BACKUP_DIR = '/usr/local/etc/mediamtx_backups'` and calls `os.makedirs(BACKUP_DIR, exist_ok=True)` at module-import time (line ~93). The systemd unit runs the editor as `User=takwerx`, but `/usr/local/etc/` is `root:root 0755` and the dir doesn't exist after deploy → `PermissionError: [Errno 13]` → editor exits 1 → `Restart=always` re-launches → tight fail-loop. Downstream: Caddy `stream.*` vhost reverse-proxies `/` to `:5080` → nothing listening → browser sees Authentik's "Not Found" page (forward_auth falls back to the auth/caddy endpoint response on upstream-unreachable, which surfaces Authentik's intermediate "application not configured" chrome — *misleadingly identical* to a true 404 from the proxy outpost). The Authentik wiring itself was 100% correct (MediaMTX provider 5 in Embedded Outpost, external_host = `https://stream.<domain>`, app `MediaMTX` slug=`stream`, 0 policy bindings = open access). Two-layer fix in `app.py` (commit `da3f7ef`): (1) `deploy_mediamtx()` pre-chowns three paths to `takwerx:takwerx` between `systemctl enable` and `systemctl start` — `/usr/local/etc/mediamtx_backups` (new dir), `/opt/mediamtx-webeditor` recursive (for the JSON config files the editor writes — theme_config, email_config, users_file, share_links, group_metadata, srt_passphrase_backup, pending_reg, reset_tokens), and `/usr/local/etc/mediamtx.yml` (editor's Save-Config UI needs write, and `mediamtx` itself runs as takwerx so this is fine for both). (2) New `_heal_mediamtx_webeditor_writable_paths()` startup migration in `_startup_migrations()` — on every console boot, if `/opt/mediamtx-webeditor` exists AND `mediamtx-webeditor.service` is loaded but NOT active, re-apply the chowns and `systemctl restart mediamtx-webeditor`. Skips entirely on healthy boxes (service active → no-op). This lets a tak-test-4-class box (fresh-deployed on v0.9.28, in fail-loop) self-heal on the next Update Now without operators having to redeploy MediaMTX from the console. Same problem class as the LE-cert read-access bug from item (5) — a `takwerx`-run service needs writable paths in `root`-owned directories, and the deploy didn't pre-create or chown them. Lesson: when adding new services that run as a non-root user, audit not just initial-launch perms but every directory the service might `mkdir` / `open(w)` over its lifetime. The forensic was a clean reproduction of the cursor rule `consult-upstream-docs.mdc` — instead of chasing the visible Authentik "Not Found" symptom (which would have led to Authentik debugging dead-ends), the upstream `mediamtx_config_editor.py` was checked first via `systemctl status` / `journalctl`, which immediately surfaced the actual traceback in 30 seconds. **Headline post-v0.9.28 hotfix:** the long-tail "8446 webadmin login fails after deploy" issue (originally documented as a v0.9.30 known-issue) was root-caused on `tak-test-4` 2026-05-18: `_apply_coreconfig_ldap_auth_et()` had `ET.register_namespace('', '')` which is the WRONG syntax to suppress the default-namespace prefix. The correct call is `ET.register_namespace('', 'http://bbn.com/marti/xml/config')`. Without proper URI registration, Python's `xml.etree.ElementTree.write()` assigns synthetic `ns0:` prefix to all 64 elements in the serialized CoreConfig — `<ns0:Configuration xmlns:ns0="...">` with `<ns0:auth>` / `<ns0:ldap>` etc. TAK Server's Spring Security LDAP wiring keys off non-prefixed `<auth>`/`<ldap>` during bean post-processing — `ns0:`-prefixed elements aren't picked up, so the OAuth2 password-grant manager initializes without an `LdapAuthenticationProvider`. The 8446 connector still binds (Tomcat-level port binding, not Spring Security state) so the box "looks healthy" — but `POST /oauth/token` returns 401 with NO LDAP bind attempt visible in the outpost logs. The previous misdiagnosis ("8446 connector silently reverts to cert_https") was an artifact of manual hand-edits during operator debugging; the real CoreConfig at deploy time still had `_name="LetsEncrypt"` — just with `ns0:` prefix that Spring couldn't parse. **Fix (three layers):** (1) detect namespace URI from file and properly register it; (2) strip any pre-existing `ns0:` prefixes from input file as self-heal for legacy boxes; (3) belt-and-suspenders verifications — abort if ET roundtrip leaves `ns0:` in output, re-read `/opt/tak/CoreConfig.xml` after `sudo cp` and refuse success if `ns0:` present, `install_le_cert_on_8446()` regex made namespace-tolerant + post-patch verify confirms `_name="LetsEncrypt"` landed. Standalone roundtrip test confirms output is clean `<Configuration xmlns="...">` form with zero `ns0:` prefixes. Operator impact: Update Now from any prior release self-heals on next LDAP sync. Manual workaround (sed-strip `ns0:` from CoreConfig + restart `takserver-api`) verified to restore 8446 login on `tak-test-4` (HTTP 200 + JWT). **Other items unchanged from prior summary:** Bundles two themes into one drop-in Update Now: (1) **Security audit response.** A friend-of-the-operator audit by "Oblivion Edge Vulnerability Research LLC / Project Shakespear" (April 2026, 74 findings, 9 CRITICAL) was cross-referenced against `infra-TAK`'s current `dev` tree. 6 of 9 CRITs target `mediamtx-installer` (different repo, forwarded). CRIT-06 (SSH probe command injection in `app.py:7932, 7962`) cited stale line numbers; current code is already mitigated by `_validate_ssh_target` (v0.9.12) + `shlex.quote` patterns. **CRIT-08 (`StrictHostKeyChecking=no` in 9 guarddog SSH calls) was real and unfixed** — `_ssh_probe` in `app.py` had been migrated to `accept-new` pre-audit, but the bash watchdogs were missed. **CRIT-07 (`eval` in 3 guarddog scripts)** had root-only blast radius (config is root-owned) but is cleaned up anyway. v0.9.29 ships: 9 guarddog scripts switched to `StrictHostKeyChecking=accept-new -o UserKnownHostsFile=/opt/tak-guarddog/known_hosts -o ConnectTimeout=10`; 2 raw-SSH callsites in `app.py` (snapshot pg_dump + rollback pg_restore) migrated for consistency; `known_hosts` file (0600, root:root) auto-provisioned in Guard Dog deploy thread AND `_sync_guarddog_remote_db_from_settings` post-config-write; operator escape hatch is `: > /opt/tak-guarddog/known_hosts` (rotation rebuilds on next watchdog cycle, no service restart). All 3 `eval "$(python3 -c ...)"` patterns replaced with newline-delimited `read` from heredoc-stdin python (no shell parsing of values ever); `eval "$cmd"` in `remote_cmd()` replaced with `bash -c "$cmd"`. New `render_default_cert_password_warning(settings)` helper renders an orange dismissable banner across every console page when `tak_cert_password` is unset or equals `atakatak` AND TAK Server is installed AND operator hasn't dismissed (`cert_pw_warning_dismissed` flag in settings.json); wired into existing `inject_cloudtak_icon` context processor; dismiss is a POST to `/api/security/dismiss-cert-pw-warning` (login-required, same-origin CSRF gate); auto-clears the dismiss flag when a non-default password is later saved via `takserver_set_cert_password` (so accidental reverts re-trigger). CSP `script-src` drops `'unsafe-eval'` (pre-flight grep verified zero `eval()` / `new Function()` / string-form `setTimeout` / `setInterval` in static JS + inline templates + app.py itself); `'unsafe-inline'` removal deferred to v0.9.30+ (requires nonce-injection across ~25 `render_template_string` callsites). New 388-line `docs/SECURITY-AUDIT-RESPONSE-2026-04.md` records per-repo split, CRIT-06 mitigation map, already-addressed-defaults table (CSRF, session flags, persistent secret_key, rate limiting, security headers), and outstanding work for v0.9.30+ (`shell=True` defense-in-depth sweep, CSP `unsafe-inline` removal, request re-baseline against current `dev`, Authentik upstream header strip in Caddy). (2) **Authentik MAX_REQUESTS cascade short-circuit (post-v0.9.28 stability).** Field forensic on tak-10 (test12) 2026-05-18 caught the v0.9.23 autotune tune-UP cascade in soak: after a real leak burst on a previous day halved MAX_REQUESTS from 1000 → 500 → 250 → 125 → 100 (floor) over ~10 minutes, the autotuner's TUNE-UP path activated 6h later when no fires were observed (`+25%/30min`). Climbing back from 100 → 1000 takes 10 steps (100 → 125 → 156 → 195 → 243 → 303 → 378 → 472 → 590 → 737 → 921 → 1000). **Each step triggers `_recreate_authentik_server_worker`** because gunicorn only re-reads `AUTHENTIK_WEB__MAX_REQUESTS` at process startup. Cost: ~7 hours and 10 unnecessary server+worker recreates, each dropping LDAP outpost websocket for 1-5s (Caddy 502s on `/api/v3/flows/executor/ldap-authentication-flow` visible in logs). The v0.9.23 PgBouncer install function (`_ensure_authentik_pgbouncer`) was *supposed* to fix this — its post-install path resets MAX_REQUESTS to baseline ("autotune floor=100 is no longer load-bearing with PgBouncer in place", app.py:27983-27986). But that reset only fires on FRESH installs; boxes upgraded-while-stuck-at-floor never got it. New idempotent startup migration `_patch_authentik_max_requests_snap_to_baseline_if_pgbouncer` (app.py:26847, wired into `_startup_migrations` after `_ensure_authentik_pgbouncer_pool_size`) closes that gap. Gating (all must be true): ~/authentik/.env exists, PgBouncer is installed in compose AND wired in .env, current MAX_REQUESTS < baseline (`authentik_max_requests_baseline`, default 1000), no SAFETY NET firing in last `_AUTHENTIK_MAX_REQUESTS_QUIET_WINDOW_S` (6h), no autotune apply in last `_AUTHENTIK_MAX_REQUESTS_TUNE_UP_COOLDOWN_S` (30min). On apply: write baseline to .env, single `_recreate_authentik_server_worker`, clear `authentik_max_requests_fire_history`, record snap in `authentik_max_requests_tune_history` with `reason='snap-to-baseline-pgbouncer-installed-quiet'`, set `last_tune_ts=now` (so autotuner won't immediately re-fire), record audit blob `authentik_max_requests_snap_migration` with `from_max`, `to_max`, `saved_recreates_estimate`. Pure-function helper `_estimate_cascade_recreates(cur, baseline)` simulates the +25%/30min step count for log-line accounting. **Field state at release time:** all 3 dev boxes (test12/test8/test6) already at MAX_REQUESTS=1000 — fix is preventive, silent no-op on current fleet, kicks in only on any future box (operator install or recovering production) that ever drops below baseline with PgBouncer present. Also ships new always-apply Cursor rule `.cursor/rules/no-main-merge-no-tag-without-permission.mdc` codifying the operator-permission gate established during the v0.9.28-alpha shipping incident (AI auto-pushed v0.9.28 to main + tagged without explicit operator request). Rule defines what counts as permission, requires explicit pre-merge prompt with branch + tag preview + impact summary, and cites the incident as the rationale. **Files touched:** 7 guarddog scripts (`tak-{remotedb-watch,retention-guard,remotedb-auth-watch,fedhub-watch,db-repack,cotdb-watch,auto-vacuum}.sh`), `app.py` (cascade-fix migration + estimator + startup wire-up + cert-password nag + dismiss endpoint + auto-clear hook + CSP drop + known_hosts provisioning in 2 places + 2 SSH option flag swaps + VERSION bump), `.cursor/rules/no-main-merge-no-tag-without-permission.mdc` (new), `docs/SECURITY-AUDIT-RESPONSE-2026-04.md` (new, 388 lines), `docs/PLAN-v0.9.29-alpha.md` (rewrote security scope + cascade fix Item 6), `docs/PLAN-v0.9.30-alpha.md` (Node-RED multipart scope rolled forward), `docs/RELEASE-v0.9.29-alpha.md` (new, ~260 lines). **Lessons:** (1) soak scripts must track recreates, not just container counts — the original 6/6 health check was lying about test12 quietly cycling server+worker every ~31 min for 10 hours; (2) when a docstring promises an acceleration path (PgBouncer install resetting MAX_REQUESTS to baseline), audit the upgrade paths too, not just the fresh-install path; (3) AI agents must always ask before main-merge or tag — codified in `.cursor/rules/no-main-merge-no-tag-without-permission.mdc`; (4) audit responses should split by repository before pricing the work — 6 of 9 CRITs in this audit weren't even in `infra-TAK`. (3) **TAK LDAP CoreConfig verifier — namespace-prefix tolerance.** Discovered 2026-05-18 on a fresh Azure deploy `tak-test-4` (on v0.9.28 main): operator connected TAK Server to LDAP via the console workflow, banner reported `coreconfig=FAIL ready=NO` but `ldapsearch -x -H ldap://127.0.0.1:389 -D cn=adm_ldapservice,…` against the running OpenLDAP container actually succeeded — LDAP was wired correctly, only the verification was wrong. Root cause: TAK Server 5.7-RELEASE-32-HEAD canonicalizes `CoreConfig.xml` on restart and writes elements with the `ns0:` XML namespace prefix (`<ns0:auth ...>...<ns0:File .../><ldap .../></ns0:auth>`). The verifier `_coreconfig_has_ldap()` was using naive `content.lower().find('<auth')` / `find('</auth>')` substring lookup which never matches `<ns0:auth>` / `</ns0:auth>`. Fixed verifier to match both shapes with `<(?:[A-Za-z][\w-]*:)?auth\b` (and closing-tag/inner-ldap variants); same namespace-tolerant regex fix applied defensively to `_apply_coreconfig_ldap_auth_text()` (legacy text-based fallback — only invoked when ElementTree parsing fails, would have silently no-op'd on namespace-prefixed CoreConfig too). The ElementTree-based primary patcher `_apply_coreconfig_ldap_auth_et()` already handled namespaces correctly (line 34337-34338 builds `ns_prefix` from root tag) — that path was what wrote tak-test-4's correct LDAP wiring in the first place. Unit-tested across 5 XML shapes before deploy (canonical-ns / plain / default=file / leftover-ldap-element / no-auth) — v0.9.28 mis-classifies the canonical-ns case (the only one fresh TAK 5.7-R32+ deploys hit); v0.9.29 returns correct truth value on all 5. (4) **Cloud public-IP detection on fresh install.** Same `tak-test-4` deploy surfaced: `start.sh` used `hostname -I | awk '{print $1}'` at two sites (settings.json `server_ip` write + openssl self-signed cert SAN generation), which on Azure/AWS/GCP returns the *private* interface IP (`10.0.0.x`), not the public IP operators browse to. Console correctly served on the public IP via the VM's NAT mapping, but the Settings UI displayed `10.0.0.x`, the SSL cert SAN encoded only `10.0.0.x`, and any downstream `server_ip`-derived URL (Caddy bootstrap, TAK Server reachability) silently inherited the wrong address. New `detect_server_ip()` helper in `start.sh` queries (in order, each with its own timeout and silent fall-through): Azure Instance Metadata Service (`169.254.169.254/metadata/instance/.../publicIpAddress`, link-local, no internet egress required), AWS IMDSv1 (`169.254.169.254/latest/meta-data/public-ipv4`, token-less, still works on older AMIs), `api.ipify.org`, `ifconfig.me`, fallback to `hostname -I`. Final result validated against IPv4 dotted-quad regex `^[0-9]{1,3}(\.[0-9]{1,3}){3}$` before being trusted. Used at both the settings.json write and openssl SAN-generation sites. Smoke-tested on Mac (correctly returned WAN IP via api.ipify after Azure/AWS IMDS timed out in 2s and fell through) and against synthetic garbage inputs (empty / HTML / DNS-strings / truncated quad). **Operator impact:** new cloud-VM installs from `start.sh` get the public IP in `settings.server_ip` and the self-signed cert SAN from first boot. Existing installs aren't auto-rewritten — operators on cloud VMs with a private IP in Settings → Server IP paste in the public IP once and Save (we don't rewrite automatically because some operators legitimately deploy on private-only LANs). (5) **MediaMTX deploy — Caddy LE cert read-access for `takwerx`.** Discovered same `tak-test-4` Azure session 2026-05-18 and independently reported by a field operator on SSD Nodes preparing for SOF Week: `deploy-mediamtx` completes all 7 steps with the "✓ SSL certificates wired" banner, then `mediamtx.service` enters a crash loop. journalctl shows `ERR stat /var/lib/caddy/.local/share/caddy/certificates/acme-v02.api.letsencrypt.org-directory/<host>/<host>.crt: permission denied` (mediamtx using Go `os.Stat`). Root cause is Caddy's documented storage model: certs are 0600 caddy:caddy under a 0700 caddy:caddy directory chain (per [Caddy storage docs](https://caddyserver.com/docs/conventions#data-directory) — "All Caddy data is stored with restrictive permissions (700) to prevent leaking secrets"). The mediamtx systemd unit runs as `User=takwerx`, which can neither traverse the chain nor read the files. Fix has three parts and ships in v0.9.29: (a) `deploy_mediamtx()` unit template gains `SupplementaryGroups=caddy` (app.py:14206). (b) After cert-wiring (app.py:14535), an explicit `usermod -aG caddy takwerx` + `chmod g+rx` on the directory chain + `chmod g+r` on the .crt/.key files + `systemctl daemon-reload`, then `systemctl restart mediamtx` picks up the new group. (c) `scripts/guarddog/tak-mediamtx-watch.sh` re-applies the chmod/usermod block right before each watchdog-triggered restart — Caddy renews LE certs every ~60 days via atomic rename which resets perms to 0600 caddy:caddy on the new files, so without renewal-resilience the operator gets a silent break ~2 months later (right when SOF Week ends for the affected deploy). Self-heals on the first guarddog restart cycle after renewal. Same Azure session also surfaced a **deferred known issue** carried forward to v0.9.30: the TAK Server 8446 LetsEncrypt `<connector>` silently reverts to `_name="cert_https"` on some installs (no `keystoreFile="certs/files/takserver-le.jks"`), causing `webadmin` 8446 login to 401 with no LDAP bind even though `<auth default="ldap"><ldap .../></auth>` is correctly wired. `install_le_cert_on_8446()` (app.py:11572) does the right thing (stops TAK Server, patches, starts), so the regression comes from a later step (LDAP sync flow re-serializing CoreConfig via ElementTree, or TAK Server's own restart-time canonicalization) dropping the LE attributes. Auto-repair migration not added to v0.9.29 because it would require stopping/starting TAK Server during boot (high blast radius). Operator workaround documented in `docs/WORKFLOW-8446-WEBADMIN.md` step 2 (stop TAK Server, edit CoreConfig 8446 connector to LetsEncrypt with `takserver-le.jks`, start TAK Server). Root-cause hunt + safe auto-repair scheduled for v0.9.30. See `docs/RELEASE-v0.9.29-alpha.md` + `docs/PLAN-v0.9.29-alpha.md` + `docs/SECURITY-AUDIT-RESPONSE-2026-04.md`.

- **v0.9.28 — Enterprise Authentik PG scaling (12-core / 48 GB minimum hardware) — RELEASED 2026-05-17 to `main` after 3 dev commits (`32131ef` core + `a01dc6e` MAX_CLIENT_CONN reconciliation hotfix + `28d0f8c` channels-baseline read fix) and field validation across 3 dev boxes (test6/test8/test12).** Sized for production-scale TAK deployments serving 100s of simultaneous clients per Authentik instance. Authentik upstream issue [#20714](https://github.com/goauthentik/authentik/issues/20714) is a confirmed bug — `django_channels_postgres` channel-layer pool exhaustion at scale, assigned to maintainer `rissson`, targeted at 2026.8.0 (~6 months out). The maintainer himself pinned the root cause to vendored library `django_channels_postgres/layer.py:174` (`max_size=4` hardcoded async DB pool per event loop × 4 gunicorn workers × 5 service containers ≈ ~100 Channels conns at baseline). Tom Endress's `anctakserver2` field forensic 2026-05-17 20:31 UTC showed `78 connections | idle | SELECT DISTINCT django_channels_postgres_groupchannel.channel FROM ...` — 78/85 pool saturation = 92% under moderate load, only 7 slots of burst headroom. v0.9.27's 300-conn pool ceiling was sized for the small-tier validation boxes (test8/responder) where the Channels-leak hotfix was developed; it's undersized for enterprise. **Sizing bumps (all coordinated):** PG `max_connections` 500 → 2000 + memory tuned for 48 GB (`shared_buffers=12GB` = 25% of RAM, `effective_cache_size=36GB` = 75% of RAM, `work_mem=16MB`, `maintenance_work_mem=2GB`, `wal_buffers=64MB`, `max_wal_size=4GB`). Autotune `PG_MAX_CONN` 500 → 2000, `CAP_PCT` 0.6 → 0.75 (effective ceiling 300 → 1500, 5× v0.9.27 headroom). Cold-start `DEFAULT_POOL_SIZE` 250 → 750 + `RESERVE_POOL_SIZE` 50 → 150 (ceiling 900 = 60% of cap). Pool floor `DEFAULT` 75 → 300 + `RESERVE` 15 → 60 (ceiling 360 — enterprise quiet boxes never shrink below this). PgBouncer `MAX_CLIENT_CONN` 1000 → 5000 (enterprise deployments have many containers × workers × outposts simultaneously). Memory budget on 48 GB box at peak: ~35-38 GB (PG shared_buffers + 1500 backends × ~10 MB + Authentik containers + OS), leaves ~10 GB headroom. **Single source of truth:** new `_AUTHENTIK_PG_COMMAND_ENTERPRISE` constant near top of app.py is the canonical PG `command:` string, referenced by install-time compose template AND both Update-Now migration paths (`_ensure_authentik_compose_patches_legacy` text-based fallback + `_ensure_authentik_compose_patches` modern YAML parse-and-mutate). No more drift across 3 inline copies as in v0.9.27. **Channels-baseline telemetry:** `_authentik_pool_autotune_compute()` now scans samples for `peak_channels_idle` + `sum_channels_idle / samples_with_classes = avg_channels_idle`, persisted to `settings.json → pool_autotune.last_decision`. Reads the existing flat `channels` key from `_authentik_pool_autotune_sample()`'s persist schema. Applied on BOTH normal compute path AND stuck-at-floor escape path. **Fleet-uniform enforcement:** removed v0.9.27's operator-override carve-out (`if max_connections > 500: preserve existing command`) per `.cursor/rules/fleet-uniform-config.mdc` — canonical enterprise command always written when current differs. Drift detection updated to consider lack of `shared_buffers` tuning as drift (any pre-v0.9.28 install). **MAX_CLIENT_CONN reconciliation in pool migration** (field hotfix #1): `_ensure_authentik_pgbouncer_pool_size()` now includes `MAX_CLIENT_CONN` in its `matches_target` check + writes fleet constant on compose mutation. Without this, a v0.9.27 → v0.9.28 update on a box whose pool size didn't drift would never recreate pgbouncer, leaving `MAX_CLIENT_CONN=1000` stuck (discovered when test6/test12 came up at the old value). **Field validation evidence:** test6 (responder, moderate prior load) at autotune-floor 300/60, peak_channels_idle=79; test8 (Channels-heavy, prior v0.9.27 cl_waiting>0 samples) at fleet-uniform cold-start 750/150 via stuck-at-floor escape, peak_channels_idle=85 (mirrors Tom's anctakserver2 78-conn observation), avg=7.1; test12 (genuinely quiet) at floor 300/60, peak=1. All 3 boxes verified at PG enterprise tuning (`SHOW max_connections;` = 2000, `SHOW shared_buffers;` = 12GB, etc.) + `MAX_CLIENT_CONN=5000` + all 18 Authentik containers healthy + zero `query_wait_timeout`. **What does NOT ship (and why):** (1) patching vendored `layer.py max_size=4 → 2` — fragile, ~40-conn savings, breaks at every Authentik upstream Update Now since the library is inside the image; (2) switching `CHANNEL_LAYERS` to Redis — Authentik 2026.2.x hardcodes the PG backend in `/authentik/root/settings.py:328-330`, `channels_redis` and the `redis` Python module aren't installed in the image, would require image fork; (3) license-cache pre-warm — Tom's forensic shows `enterprise/license` cache dominates 61% of idle conns but pre-warming patches Authentik's internal cache-key format (unstable across releases), deferred to v0.9.29 only if v0.9.28 shows under-sizing; (4) worker-count reduction — wrong direction for enterprise throughput. **Lessons:** (1) capacity is sometimes the simplest fix — for a 6-month upstream fix horizon, a 5× capacity headroom multiplier is more robust than fragile interceptions; (2) always read the upstream issue thread before designing a fix — the Authentik maintainer's `layer.py:174` pin and endorsement of capacity bumps would have been invisible from a symptoms-only investigation; (3) field validation finds bugs local testing can't — the MAX_CLIENT_CONN reconciliation gap and the channels-baseline schema mismatch were both invisible until the migration ran against a real install with carried-over v0.9.27 samples; (4) single-source-of-truth constants eliminate drift — the 3-inline-copies pattern of v0.9.27 left room for one copy to silently fall behind. See `docs/RELEASE-v0.9.28-alpha.md` and `docs/PLAN-v0.9.28-alpha.md`. Plan moves: previous v0.9.28 (Node-RED ArcGIS multipart polygon) → v0.9.29; scope unchanged across move.

- **v0.9.27 — PgBouncer pool autotune + self-healing Channels-ghost reaper (kill v0.9.26 fleet fracture) — RELEASED 2026-05-17 to `main` after four hotfix iterations on `dev` and field-validation across three test boxes (tak-10 + test8 + responder), 20-min final soak with all 18 Authentik containers healthy and zero query_wait_timeouts.** v0.9.26 codified `DEFAULT_POOL_SIZE=75` and used `max(cur, target)` in `_ensure_authentik_pgbouncer_pool_size` to "never lower an operator override" — silently letting tak-10 (operator-typed 250/50 from morning's troubleshooting) stay at 250/50 while test8 (no override) got the codified 75/15 → 297 `query_wait_timeout` events in 5 min, server-1 unhealthy. **I had validated tak-10's 65-min stability before shipping v0.9.26 to main, but tak-10's runtime was operator-overridden 250/50; the codebase shipped 75/15. No box anywhere actually ran the codebase default before I shipped it.** Operator's framing: *"we cant be having one box one may and another another way because of how we tested."* v0.9.27 ships in four hotfix iterations, each a failure mode discovered live during dev validation. **Sizing formula (initial):** `peak = max(idle_count) over last 30 watchdog samples ≈ 1 h`; `target_ceiling = max(FLOOR_CEILING=90, ceil(peak × SAFETY_FACTOR=2.0))`; capped at `PG_MAX_CONN=500 × CAP_PCT=0.6 = 300`; split 5:1 default:reserve. Migration **always writes** the autotune output — no `max(cur, target)`. **Hotfix #1 — cold-start fleet constant + NameError fix.** Hotfix #0's single-shot live `pg_stat_activity` seed sampled tak-10 mid-Authentik-restart (idle=9 during traffic lull) → autotune mathematically correct but shrunk tak-10 to FLOOR 75/15. Replaced with a known-safe fleet constant: cold-start (samples < 3) always returns 250/50 (ceiling = PG_MAX_CONN × 0.6). Every box reboots into this on fresh ring; autotune fine-tunes on next reboot. Also removed a stale `_reconcile_watchdog_threshold(new_default, new_reserve)` NameError. **Hotfix #2 — cl_waiting demand telemetry + stuck-at-floor escape.** Idle-count alone measures pool *headroom*, not *demand* — a starved box has all conns checked out, low idle, autotune confirms small pool is "fine" (self-reinforcing under-sizing). New `_authentik_pgbouncer_cl_waiting()` runs `SHOW POOLS` inside the pgbouncer container (auth via `$DB_PASSWORD` from container env) for `cl_waiting + cl_waiting_cancel_req` — clients literally queued waiting for an upstream conn, cannot lie under starvation. Watchdog tick samples cl_waiting alongside idle. Autotune compute adds escape: if ≥ 3 samples have cl_waiting telemetry AND any > 0, force fleet safe-constant 250/50 regardless of idle peak. **Witnessed firing twice in production on test8.** **Hotfix #3 — continuous in-place remediation.** Hotfix #2's compute escape only ran at console restart; a quiet box getting a load spike would emit query_wait_timeouts until operator Update Now (unbounded latency). Watchdog tick now triggers `_ensure_authentik_pgbouncer_pool_size()` directly when sustained starvation: 3 consecutive 2-min ticks with cl_waiting > 0 AND current_pool < 250 AND ≥ 15 min since last in-place resize (cooldown via `settings.pool_autotune.last_inplace_resize_ts`) → pgbouncer recreate in ~30s, no operator action. **Detection-to-remediation: ~6.5 min worst case.** Triple-nested try/except so any failure is swallowed and the original `idle > threshold → restart server-1` safety net always runs. **Hotfix #4 — ghost-Channels-conn reaper.** test8 forensic 2026-05-17 22:14 UTC revealed 290 `SELECT DISTINCT django_channels_postgres_groupchannel` backends frozen in `state='idle'` for 2h 2min with consecutive PIDs. Pattern: Authentik server-1 opens Channels long-poll subscriptions per websocket client; when server-1 is killed/restarted (any pgbouncer recreate, any watchdog safety-net restart, any Update Now), listener Python processes die but PostgreSQL backends don't see a disconnect because PgBouncer holds the socket in transaction mode. Pool fills with ghosts. **Manual test: pg_terminate_backend on the narrow ghost pattern killed 290 conns, idle 299→11, all 6 Authentik containers healthy in <60s.** New `_authentik_reap_ghost_channels_conns()` runs the recovery SQL (`datname='authentik'` + `state='idle'` + `query LIKE '%groupchannel%'` + `state_change < NOW() - INTERVAL '5 min'`) from BOTH startup migration (self-heals every v0.9.26 → v0.9.27 Update Now) AND watchdog tick on `cl_waiting > 0` or `channels_idle > pool_ceiling/2` (40-min cooldown via `last_ghost_reap_ts`). This is an upstream Authentik + django_channels_postgres pattern, NOT introduced by v0.9.27 — but every v0.9.27 install will hit it on Update Now, so we ship the cleanup. **Sampling architecture:** `_authentik_pool_autotune_sample(idle, classes, cl_waiting)` appends `{t, idle, channels, dramatiq, cache, advisory_lock, other, cl_waiting}` to `settings.pool_autotune.samples` (ring buffer, last 30 entries FIFO). Backwards-compatible: legacy samples without `cl_waiting` don't trip the escape. **Apply path:** rewritten `_ensure_authentik_pgbouncer_pool_size(plog, target=None, target_reserve=None)` calls compute (or uses explicit target if passed) → always writes target to compose YAML, no `max(cur, target)`. Recreates pgbouncer only if `cur != target` (idempotent). Reconciles watchdog threshold to `ceiling + 50` bidirectionally. **Audit trail:** every decision in `settings.pool_autotune.last_decision` (`computed_at`, `version`, `samples_seen`, `peak_observed`, `peak_at`, `peak_cl_waiting`, `peak_cl_waiting_at`, `samples_with_cl_waiting`, `target_*`, `capped_at_pg_max`, `reason`, `seed_source` ∈ {`fleet_cold_start_constant`, `cl_waiting_escape`, absent for normal autotune}); `last_applied` records compose write; `last_inplace_resize_ts/_result/_trigger_cl_waiting`; `last_ghost_reap_ts/_count`. **New cursor rule `.cursor/rules/fleet-uniform-config.mdc` (always-apply):** codifies fleet-uniform principle, forbids `max(cur, target)` override-preservation, requires minimum 3-box dev-channel validation + ≥ 20-min soak before any main merge touching fleet-wide configuration. **Field validation evidence:** Three test boxes, 4 dev commits (`70ebeab` → `82a6521` → `b324c17` → `335bc15` → `3b75b87`), 20-min final soak post-hotfix-#4. All 18 Authentik containers (3 × 6) report `healthy, streak=0`. Zero tracebacks. Zero query_wait_timeouts. Every hotfix path observed firing correctly at least once: hotfix #1 cold-start constant on initial deploys; hotfix #2 compute escape fired TWICE on test8 with `peak_cl_waiting=4 > 0 over 30 samples → forcing 250/50`; hotfix #3 in-place remediation armed and dormant (correct — no sustained starvation observed in soak); hotfix #4 reaper ran on all three boxes (post-recovery: "no ghosts found (clean)") + manual test on test8 killed 290 ghosts. test8 went from "every Authentik container unhealthy, 290 ghosts, 18 qwt/10min" pre-recovery → "every container healthy, 0 ghosts, 0 qwt" 20 min post-hotfix-#4 through the v0.9.27 code path. **Lessons recorded:** (1) operator overrides are temporary, not architectural; (2) validation must happen on a box where runtime config == codebase output; (3) single-box validation is forbidden for any release touching fleet-wide config (3 boxes, ≥ 20 min soak, no manual edits); (4) measure demand, not just headroom — idle-conn is a headroom signal that goes DOWN under starvation; (5) detection-to-remediation latency matters more than detection coverage — self-healing means closing the loop, not just observing it; (6) upstream-bug self-healing is a release feature — every v0.9.27 install will hit the Authentik+django_channels_postgres ghost-conn pattern on Update Now, so the release ships the cleanup; (7) class-of-bug: any time a migration uses `max(cur, target)` or `min(cur, target)`, ask "does this create silent fleet drift?" — the answer is almost always yes; (8) tunnel-vision is a forensic anti-pattern — when a soak passes on N-1 of N boxes, the Nth box is the story, not noise. See `docs/RELEASE-v0.9.27-alpha.md` + `.cursor/rules/fleet-uniform-config.mdc`.
- **v0.9.26 — Authentik task-log purge regression fix + post-DELETE deep compaction + PgBouncer pool re-bump (35 → 75) + vm.overcommit_memory + watchdog class-breakdown + watchdog-threshold-scales-with-pool-ceiling amendment — RELEASED 2026-05-17 to `main`, four hotfix waves + one amendment, field-validated on `tak-10` with 1 h 5 min continuous stability (16:50–17:54 UTC, zero `query_wait_timeout`, zero `context canceled`, all 6 containers healthy, `/-/health/live = 200 in ~100 ms`). Hotfix #3 specifically closes the Django Channels groupchannel pool-eater class: 17 PG-backed long-polling SELECTs consumed half a 35-slot pool, triggering `query_wait_timeout` storms 90 min after every server recreate. Bumped to 75/15 (90-conn ceiling) with idempotent `_ensure_authentik_pgbouncer_pool_size` migration handling both DEFAULT and RESERVE values. Hotfix #4 incorporates Tom Endress's anchortak 2026-05-17 incident report: new `_ensure_vm_overcommit_memory(plog)` startup migration sets `vm.overcommit_memory = 1` runtime + persisted to `/etc/sysctl.conf`, preventing Redis BGSAVE fork failures that would cascade into Authentik server hangs on `cache.set()`. ak-pg-watchdog enhanced with `_classify_idle_load()` that breaks down idle connections by query class (`Channels=89 dramatiq=14 cache=22 advisory_lock=3 other=34 (dominant=Channels)`) on every ALERT or ACCUMULATING fire — gives operators class-level visibility into pool eaters without ad-hoc psql. Documents Tom's specific failure mode (4 cron tasks coinciding at 05:30 UTC → 36 dramatiq threads racing on `_fetch_pending_messages` `SELECT FOR UPDATE` without `SKIP LOCKED` → row-level lock pile-up → pool exhaustion → cascade) AND tak-10's parallel Channels-class failure (4 workers × 36 channels = 144 long-polling SELECTs, structurally) in a single forensic narrative — both classes mitigated by `vm.overcommit_memory=1` + sufficient pool ceiling (75/15 default; operator-override-preserving migration ensures tak-10's 150/30 is not silently lowered).** Closes a long-running silent failure in `takauthentiktasklogpurge.service` (the v0.9.5 Guard Dog weekly Sunday 03:00 timer). On `tak-10` 2026-05-17 03:00:06 UTC the service ran in 6 seconds and exited 1 with `ERROR: VACUUM cannot run inside a transaction block`; the purge log showed `DELETE 0 / DELETE 0 / ERROR`. Two compounding bugs in the v0.9.5 script: **Bug 1 — VACUUM in transaction.** Original used `psql -c "DELETE; DELETE; VACUUM"`; psql wraps multi-statement `-c` in an implicit transaction, VACUUM is forbidden inside transactions → exit 1 every Sunday for ~6 months on every install. **Bug 2 — 30-day hardcoded window.** Threshold `mtime < NOW() - INTERVAL '30 days'` matches zero rows when bloat is recent (e.g. 24h of release-day restart-churn from the v0.9.25 arc) → `DELETE 0 / DELETE 0`. The combined `authentik_tasks_task` + `authentik_tasks_tasklog` tables on tak-10 reached **~960 MB / 2.66M rows** (442 MB / 640,596 + 516 MB / 2,025,958) — every Authentik request through Django middleware (license check, RBAC, events, brands, root) got too slow to complete in the asgiref timeout window → `context canceled / CancelledError exception in shielded future` cascade → `authentik-server-1` pinned at sustained ~80% CPU and load average climbing 6.75/4.74/3.58. PG layer was *healthy* (39 idle / 1 active / 1 idle-in-tx; PgBouncer doing its job; `django_postgres_cache_cacheentry` at 816 KB / 68 rows — confirming this was **not** the v0.9.21 upstream license-cache leak class). Fix is three-part, all unconditional in `_startup_migrations`: **Item 1 — `_AUTHENTIK_TASKLOG_PURGE_SCRIPT` canonical constant.** Single source of truth defined near `_self_heal_authentik_compose`. Multi-tier deletion ladder (Tier 1 7d → Tier 2 24h → Tier 3 1h, each tier only fires if combined size still ≥ 100 MB after the previous). DELETE and VACUUM ANALYZE run in **separate** `psql -c` invocations. `set -u` instead of `set -euo pipefail` (each tier handles its non-zero return explicitly). Both embedded write sites at `app.py:6488` and `app.py:7004` now reference the shared constant. **Item 2 — `_ensure_authentik_tasklog_purge_script(plog)`.** Runs on every console boot; compares the on-disk script at `/opt/tak-guarddog/tak-authentik-tasklog-purge.sh` against the canonical content and overwrites drift. Closes the v0.9.5 "emit once at install, never update" landmine. Idempotent silent no-op when content matches. **Item 3 — `_auto_authentik_tasklog_purge(plog)`.** Inline Python cleanup mirroring the on-disk script's tier ladder via `subprocess.run` calls to `docker exec authentik-postgresql-1 psql`. Runs unconditionally on every console boot. Doesn't depend on the systemd timer firing OR the on-disk script being correct. Idempotent silent no-op when combined size < 100 MB. Logs initial+final sizes and row counts on every cleanup run. Updates `/opt/tak-guarddog/authentik_tasklog_purge_last.txt` so the Guard Dog dashboard reflects inline runs. Non-raising — exceptions logged and swallowed. Acceptance plan: `_startup_migrations` log on first v0.9.26 console boot shows `Authentik tasklog purge script updated to v0.9.26 canonical version` + `Authentik tasklog: <N> MB ... — running v0.9.26 cleanup` + `cleanup complete — <N> MB → <M> MB (freed <D> MB)`; `docker stats authentik-server-1` drops from 80% sustained to single-digit %; `journalctl -u takwerx-console | grep -cE 'context canceled|CancelledError'` returns to 0; next Sunday 03:00 `takauthentiktasklogpurge.service` shows `Active: active (exited)` not `failed`. v0.9.25's `cap_drop` heal is unchanged — purely additive on top. Plan moves: previous v0.9.26 (DataSync mission lifecycle) → v0.9.27; previous v0.9.27 (Node-RED ArcGIS multipart polygon) → v0.9.28. Both moved verbatim, no scope changes. **Lessons recorded:** (1) emit-once-at-install on-disk script patterns are landmines — add `_ensure_<script>_canonical(plog)` migrations on every boot for anything in `/opt/tak-guarddog/*.sh`; (2) `set -euo pipefail` + `psql -c "DELETE;DELETE;VACUUM"` is a footgun stack — VACUUM never runs inside the implicit transaction; (3) don't trust timer existence as proof it works — `systemctl status <service>` is the ground truth, and the stamp file write must happen on every exit path including healthy-no-op; (4) hygiene migrations that need guaranteed reach live in `_startup_migrations`, not `_post_update_auto_deploy`; (5) 80% Authentik server CPU + `CancelledError` in Django middleware ≠ the v0.9.21 enterprise-license-cache leak class — always check ALL Authentik table sizes (`authentik_tasks_*`, `authentik_events_event`, `django_channels_postgres_message`, `django_postgres_cache_cacheentry`) BEFORE diagnosing; (6) one release day's worth of `systemctl restart takwerx-console` cycles can stuff ~1 GB into `authentik_tasks_*` — plan for it when iterating heal logic across many same-day pushes; (7) **safety nets that aren't proportional to their environment are themselves bugs** — the hardcoded 150-idle threshold in `ak-pg-watchdog` was correct for v0.9.23 (ceiling 30), v0.9.24 (40), and v0.9.26 hotfix #3 (90), and BROKEN for tak-10's operator-overridden ceiling 180 — closed by `_reconcile_watchdog_threshold` (Hotfix #4 amendment) which makes the threshold scale with the pool ceiling; **audit similar coupled-pair constants for the same class of bug**; (8) **symptom unification can hide compound bugs** — hours of "tak-10 keeps failing the same way" turned out to be TWO bugs braided together (pool too small for Channels load + watchdog firing on healthy pool); the watchdog class breakdown (`Channels=102 dramatiq=0 ... dominant=Channels`) made the second failure mode visible — the safety net was citing Channels as the culprit, but the safety net itself was the culprit; **when a single failure narrative explains "everything," widen the diagnostic surface**.
- **v0.9.25 — Authentik `cap_drop` duplicate-key heal + Sync webadmin recovery + compose-heal stamp diagnostics — RELEASED 2026-05-16, six in-release hotfixes same day, confirmed working on `aj.takdfir.com` / tak-10 with operator "ok it worked."** Closes a self-inflicted YAML regression from the v0.9.x Phase 2 container-hardening injector in `_auto_harden_containers()` which could write `~/authentik/docker-compose.yml` with two `cap_drop:` mapping keys inside the same Authentik `server:` service (the original injector used a whole-file literal-substring guard `_new not in _ak` that returned False whenever the existing `cap_drop:` lived in any position other than the one the literal substring described — exactly where the embedded template places it). YAML rejects duplicate mapping keys → `docker compose up -d --force-recreate ldap` (used inside Sync webadmin to flush the LDAP outpost bind cache) bails on parse → operator stuck behind a red `Password set but LDAP restart failed: yaml: construct errors: ... mapping key "cap_drop" already defined ...` banner with no recovery path short of SSH'ing in and hand-editing YAML. **Initial release** shipped a substring-based `_dedupe_authentik_capdrop()` + section-bounded server-section injector rewrite + the 120-char error truncation removed + a duplicate-key remediation hint + a `_validate_authentik_compose()` pre-flight gate + fixed the Sync webadmin card description to match its actual behavior (it DOES recreate the LDAP outpost — previous text said "Does not restart anything"). **Hotfix #1** added a PyYAML round-trip inside `_auto_harden_containers()` because tak-10's compose file had two `cap_drop:` blocks at *different indentation dialects* (4-space and 6-space dash indent) that no substring match could heal — `yaml.safe_load` resolves duplicate keys via last-wins, `safe_dump` re-emits canonical indentation. Same parse-and-mutate pattern v0.9.21 already used in `_ensure_authentik_compose_patches`; should have been the dedupe approach from day one. **Hotfix #2** extracted that heal into a new module-level `_self_heal_authentik_compose()` and called it from `_post_update_auto_deploy`'s same-version branch + the local-target path of `_ensure_authentik_webadmin` (so Sync webadmin self-heals on click). Failed in field test because `_post_update_auto_deploy` has its own gating logic (single-flight lock, post-update lock); not guaranteed to run on every restart. **Hotfix #3** moved the heal call into `_startup_migrations()` as the **first Authentik-touching call**, unconditional on every console boot, BEFORE all other Authentik migrations (every one of `_ensure_authentik_compose_patches`, `_ensure_authentik_pg_persistent_connections`, `_ensure_authentik_pgbouncer`, `_ensure_authentik_pgbouncer_pool_size`, etc. calls `docker compose up …` which hard-fails on parse errors). Added a `Startup migration: console boot — VERSION=… git=<short-sha>` diagnostic boot log line so operators can grep journals for the loaded commit SHA. Also added retry-after-heal inside `_ensure_authentik_webadmin`'s LDAP recreate path. **Hotfix #4** added a `POST /api/authentik/compose-heal` manual operator endpoint (calls `_self_heal_authentik_compose()` + force-recreates LDAP outpost on heal-success) for "every automatic path got skipped somehow and I need YAML fixed RIGHT NOW" recovery. Five heal entry points after this: `_startup_migrations` (unconditional), `_auto_harden_containers` (version-change), `_post_update_auto_deploy` same-version branch, `_ensure_authentik_webadmin` (Sync click), and the manual API endpoint. **Hotfix #5 (diagnostics, the breakthrough)** added a stamp file at `/var/lib/takwerx-console/authentik-compose-heal.last` written on EVERY heal call regardless of outcome — contains `timestamp_utc=`, `version=`, `git=<short-sha>`, `result=<one-line status>`. New helper `_read_compose_heal_stamp()` formats this with a human-readable age (`30s ago`, `2h ago`, `never written on this box`). `_format_ldap_restart_err()` now appends the stamp line to every Sync webadmin failure banner. Net effect: the FAILURE BANNER ITSELF self-describes whether the heal is running, what it returned, how recently, and from which git SHA. Distinguishes 4 different failure shapes (heal-not-loaded vs heal-running-but-broken-result vs heal-validating-as-bad vs old-code-still-loaded) at a glance — no SSH into the box required. **Hotfix #6 (ROOT CAUSE)** — the stamp evidence from #5 (`result='compose self-heal: canonical YAML failed validation (error while interpolating services.postgresql.environment.POSTGRES_PASSWORD: required variable PG_PASS is missing a value: database password required) — left disk file unchanged'`) revealed the real bug: `_validate_authentik_compose()` was running `docker compose -f /tmp/<tmp>.yml config` against the candidate file. `docker compose config` interpolates env vars from `.env` in the directory of the `-f` argument — `/tmp/.env` doesn't exist on tak-10, the env-var refs in the canonical YAML (`${PG_PASS}`, `${AUTHENTIK_SECRET_KEY}`, etc.) failed interpolation, validator returned False, heal helper followed its safety contract and left the broken file on disk unchanged. **The heal logic was correct from hotfix #1 onward — it was the validator that was the false negative.** Five iterations of "logically correct fix" were silently rejected by an unrelated runtime concern. Fix: replaced the `docker compose config` shell-out with a **PyYAML strict reparse** using a custom `_no_dupe_construct_mapping` constructor that raises on duplicate keys — checks structure (duplicate keys, syntax) without any runtime / `.env` / `/tmp` dependency, matches docker compose's Go YAML parser's hard failure mode (`gopkg.in/yaml.v3` also rejects duplicate keys). Operator clicked Sync webadmin after pulling: **"ok it worked."** First time across six hotfixes. **Lessons for the next dev:** (1) **When "logically correct fix" meets "same error symptom" three or more times in a row, stop iterating on the logic and instrument the failure.** Hotfix #5's stamp file was the right move two hotfixes earlier than it landed — every iteration between #2 and #4 was a refinement of code blocked by a totally different concern the whole time. One ~30-line diagnostic write shortcut the loop. (2) **`docker compose config` interpolates env vars from `.env` in the directory of the `-f` argument** — running it against a temp file in `/tmp/` will fail interpolation when the candidate references env vars defined in the real install's `.env`. For structural-only YAML checks (duplicate keys, syntax), use a PyYAML strict reparse with `_no_dupe_construct_mapping`. Reading the upstream `docker compose` doc page before wiring `docker compose config` as the validator would have caught the false-negative class up front (see `.cursor/rules/consult-upstream-docs.mdc`). (3) **The architecturally correct home for "must-run-every-boot" heals is `_startup_migrations()`.** Both `_post_update_auto_deploy` and `_auto_harden_containers` have legitimate gating that can skip them. Heals that protect every other downstream Authentik migration (all of which call `docker compose up …` and hard-fail on YAML parse errors) belong in the unconditional startup block as the FIRST Authentik-touching call. Same lesson as v0.9.20's wiring-gap follow-up. (4) **Substring-based YAML manipulation is brittle across indentation dialects.** `yaml.safe_load` / `yaml.safe_dump` is the right tool — substring dedupe stays as defensive second pass for hosts without PyYAML, but is now a no-op on canonicalized files. (5) **The compose-heal stamp file at `/var/lib/takwerx-console/authentik-compose-heal.last` stays as permanent diagnostic instrumentation** — any future regression in this area will surface in the failure banner without operator action. See `docs/RELEASE-v0.9.25-alpha.md` (six addenda, ~330 added lines of forensic trace + lessons) and `docs/PLAN-v0.9.25-alpha.md`.

- **v0.9.24 — Update Now single-flight lock + post-update service recovery sweep + PgBouncer pool headroom (25 → 35) — RELEASED 2026-05-16, hotfix on top of v0.9.23 after one day of field data.** Three fixes driven directly by the first 24 hours of v0.9.23 in production: **Item 1 (Update Now single-flight lock):** `/api/update/apply` writes `/var/lib/takwerx-console/update-now.lock` (version + UTC + PID) on entry; concurrent POSTs return `HTTP 409 {in_progress: true, started_seconds_ago, error}` and the UI handles it with a friendlier cyan "Update in progress…" state + 30s auto-reload. Lock cleared in `_run_post_update_guarded` `finally:` (covers normal + crash paths), in the `_post_update_auto_deploy` no-op early-return when a lock is present on same-version restart (also runs the recovery sweep), and via a 20-min TTL (wedged-process safety valve). Closes the test8 (ssdnodes-66871ef1e08d7) race observed 2026-05-16 where the operator double-clicked Update Now from two browser tabs; both POSTs ran in parallel, both scheduled `systemctl restart takwerx-console`, the second restart fired ~400ms after the first and killed the migration thread mid-bootstrap → `tak-portal` + `mediamtx` ended `Exited (0)` (Docker `restart: unless-stopped` does NOT restart explicitly stopped containers) for 12+ min until manual recovery. **Item 2 (post-update service recovery sweep):** new `_post_update_service_recovery_sweep(plog)` enumerates Docker containers via `docker ps -a` and explicitly starts any in `exited`/`created`/`dead` state matching known infra-TAK name prefixes (`authentik-`, `cloudtak-`, `tak-portal`, `mediamtx`, `fedhub-`, `caddy`, `nodered`, etc.), then walks a conservative systemd unit list (`takserver`, `mediamtx` native, `nodered`, `takmediamtxguard.timer`, `takremotedbguard.timer`, `takremotedbauthguard.timer`) starting any that are `loaded; inactive` / `failed` but still `enabled`. Idempotent (`docker start` on running = no-op, `systemctl start` on active = no-op). Called from end of `_run_post_update()` + from no-op early-return when an Update Now lock is present. **Item 3 (PgBouncer pool bump 25 → 35):** field data from tak-10 (2026-05-16, first full day on v0.9.23 PgBouncer) showed `SHOW POOLS` saturated at `sv_active=29/sv_idle=1` under modest concurrent load (84 client conns, `maxwait=0` but zero spike headroom). `_AUTHENTIK_PGBOUNCER_DEFAULT_POOL_SIZE` bumped 25 → 35 (ceiling 30 → 40 with RESERVE=5, still ~12% of `max_connections=500`, ~33% spike headroom over observed peak). New `_ensure_authentik_pgbouncer_pool_size(plog, target=None)` migration wired into `_startup_migrations` right after `_ensure_authentik_pgbouncer`; patches existing v0.9.23 compose YAML in place (backup to `*.poolsize-bak-<ts>`) and force-recreates only the `pgbouncer` container (server+worker keep their client conns; PgBouncer healthcheck cycling re-pools them within seconds). Operator overrides above 35 are preserved (idempotent skip + log). **Sidebar — Server One UFW manual fix (documented, deferred to v0.9.25 in code):** during v0.9.24 dev on test8 found that v0.9.12 hardening's UFW rules on Server One DB hosts can end up in wrong order — generic `8080/tcp DENY Anywhere` at position N, scoped `8080/tcp ALLOW from <consoleIP>` at position N+1 → UFW first-match-wins means DENY hides ALLOW → port 8080 unreachable, Guard Dog dashboard tile red. Manually fixed on Server One (`tak-11720985703`, 190.102.110.222) via `ufw delete 6; ufw insert 5 allow from 63.250.55.132 to any port 8080 proto tcp` — verified `curl http://190.102.110.222:8080/health` returns `{"healthy": true}` in 254ms. Code-level fix (`ufw allow ...` → `ufw insert 1 allow ...` in `app.py` lines 4100/7168/41413) deferred to v0.9.25 to keep v0.9.24 tightly scoped. **Lessons:** (1) field validation > theoretical sizing — v0.9.23's pool ceiling was sized from another project's RDS deployment; tak-10 hit 29/30 on day one. Size from your own steady-state peak + ~33% headroom. (2) `restart: unless-stopped` only restarts containers killed by Docker daemon failures or non-zero exits, NOT SIGTERM during graceful shutdown — recovery for the SIGTERM path needs explicit application logic. (3) Single-flight locks belong at the API door, not in the work loop — by the time post-restart code runs, both restart calls have already been scheduled. (4) UFW is first-match-wins; rule order is as important as rule content. See `docs/RELEASE-v0.9.24-alpha.md` and `docs/PLAN-v0.9.24-alpha.md`.

- **v0.9.23 — Authentik PG connection-leak class closed STRUCTURALLY via PgBouncer + TAK Server connection-state diagnostic rewritten against the actual cot DB schema — RELEASED 2026-05-15, validated on tak-10 (test12.taktical.net, Authentik 2026.2.x, PgBouncer v1.25.1-p0).** Closes the chain v0.9.20 → v0.9.21 → v0.9.22 chased through three different client-side levers (`CONN_MAX_AGE`, `idle_session_timeout`, worker `MAX_REQUESTS` autotune). Root cause confirmed against upstream Authentik [#20714](https://github.com/goauthentik/authentik/issues/20714) and our forensic: `django_postgres_cache` cache-miss path bypasses Django's `CONN_MAX_AGE` request-end cleanup, and Authentik 2026.x's async-to-sync thread pool abandons threads without triggering the cleanup hook. **No client-side tuning can fix upstream code** — the architectural answer is a connection pooler. **Phase 6:** `_ensure_authentik_pgbouncer(plog)` adds `edoburu/pgbouncer:v1.25.1-p0` to `~/authentik/docker-compose.yml` (transaction pool mode, scram-sha-256, `DEFAULT_POOL_SIZE=25 + RESERVE_POOL_SIZE=5 = 30 real PG conn ceiling`, `MAX_CLIENT_CONN=1000`, `SERVER_RESET_QUERY=DISCARD ALL`). Five Authentik env vars wired into `.env`: `AUTHENTIK_POSTGRESQL__HOST=pgbouncer`, `__PORT=5432`, `__DISABLE_SERVER_SIDE_CURSORS=true` (REQUIRED for transaction mode per [Authentik docs](https://docs.goauthentik.io/install-config/configuration/#using-a-postgresql-connection-pooler) — server-side cursors maintain state across queries which transaction-mode pooling breaks), `__CONN_HEALTH_CHECKS=true`, `__CONN_MAX_AGE=0`. **v2.2 compose-precedence fix — the part that actually makes it work:** Authentik's upstream compose template hardcodes `AUTHENTIK_POSTGRESQL__HOST: postgresql` in `services.{server,worker}.environment`, and **per Docker Compose semantics `environment:` takes precedence over `env_file:`** — so the v1 install's `.env` rewrite was silently overridden every container recreate. The v1 post-install probe missed it because of an IP-mapping assumption error (`172.19.0.3`/`.4` thought to be PgBouncer-multiplexed pools when in fact server/worker were connecting direct to postgresql; PgBouncer was actually at `172.19.0.7`). v2.2 detects the partial-install state (`PARTIAL INSTALL DETECTED` in migration log), patches `services.{server,worker}.environment.AUTHENTIK_POSTGRESQL__HOST` `postgresql` → `pgbouncer`, force-recreates server+worker. **Tak-10 v2.2 evidence:** post-fix `pg_stat_activity` shows ONE row with `client_addr=172.19.0.7` (PgBouncer) and count=7 — no direct conns from server (was 9 idle) or worker (was 6 idle). `SHOW POOLS` confirms multiplexing: `cl_active=28 sv_idle=3 sv_used=4 maxwait=0` — 28 client conns parked at PgBouncer, brokered through 7 real PG conns (4:1 ratio in steady state). `SHOW STATS` shows `total_query_count` growing at ~1,271 queries/min through PgBouncer with `avg_wait_time=2µs`. **Phase 6b TAK Server connection-state diagnostic (v2):** new `_takserver_connection_state` helper + `GET /api/takserver/zombies` (kept as alias) and `GET /api/takserver/connection-state` (canonical). Queries the local cot DB directly via `sudo -u postgres psql cot` — NO mTLS, NO admin cert passphrase, NO Marti API. Returns `currently_connected` / `currently_disconnected` from last event per identity, `events_last_5min/1h/24h` from audit log, sample of top-10 connected clients, advisory in `HEALTHY` / `IDLE` / `QUIET` / `DORMANT` / `INACTIVE`. **Original v1 "zombie subscription" framing was a misdiagnosis:** `client_endpoint` is TAK Server's immortal audit log (`ON DELETE RESTRICT` FK from `client_endpoint_event` — TAK Server is explicitly preserving the trail), NOT a runtime pool. Rows persist across JVM restarts (proven on tak-10: 27 pre-restart, 27 post-restart, same UIDs). Marti's `lastEventTime: null` means "currently disconnected", not "zombie" (proven empirically: device D8985041-... reported as null at 13:31 PT, connected one minute later at 13:32, with 7+ daily connect/disconnect cycles in audit log). `sudo systemctl restart takserver` does NOT clear these rows. `POST /api/takserver/zombies/sweep` retired → returns 410 Gone. **v2.1 advisory correction (same evening):** v2 fired `ATTENTION` on a healthy box because audit-log silence in last 5 min was treated as routing-impairment signal — but `client_endpoint_event` records state transitions only, not CoT traffic. Stably-connected clients generate zero rows during session. v2.1 simplifies: if `currently_connected > 0`, advisory is `HEALTHY` (no further checks). CoT-routing impairment shows up in `takserver-messaging.log`, not in audit tables. **Standalone operator scripts:** `ops/diagnostics/anchortak/zombies.sh` + `zombies.py` ship paste-free DB-backed diagnostic. **Watchdog repositioned:** `_authentik_channels_pool_watchdog_loop` retained as defense-in-depth (catastrophic regression catch only) — PgBouncer now enforces the connection ceiling architecturally. **Lessons for the next dev who touches PgBouncer or compose env vars:** (1) `environment:` in docker-compose.yml takes PRECEDENCE over `env_file:` — always validate with `docker exec <container> env | grep <VAR>`. (2) PgBouncer post-install probes must validate by `client_addr` (resolve pgbouncer container IP via `docker inspect`), not by `application_name` (which PgBouncer doesn't propagate for Authentik 2026.2.x). (3) `SHOW POOLS` only shows pools that have ever received a client connection — an empty pool with a configured DB row in `SHOW DATABASES` but no row in `SHOW POOLS` = you're being bypassed. (4) TAK Server's `client_endpoint*` tables are an audit log, not a runtime pool — don't derive "live subscription count" from them. (5) A diagnostic that fires `CRITICAL` on a healthy box is worse than no diagnostic — validate threshold logic against the real data model before shipping alarm copy. See `docs/RELEASE-v0.9.23-alpha.md` and `docs/PLAN-v0.9.23-alpha.md`.

- **v0.9.22 — Authentik hotfix release: remove idle-session regression + enforce proxy host convergence on Update Now — RELEASED 2026-05-14, validated on tak-10 (test12.taktical.net).** Follow-up to v0.9.21 field regressions. **Part 1 (runtime stability):** removed `idle_session_timeout` from Authentik Postgres command targets (fresh template + YAML patcher + legacy patcher) because real-world validation showed it kills `django_channels_postgres` LISTEN sockets and causes reconnect storms (`Postgres connection is not healthy`, `IdleSessionTimeout`) with major CPU spikes. Keep safe knobs: `max_connections=500`, `statement_timeout=120s`, `idle_in_transaction_session_timeout=300s`, `CONN_MAX_AGE=10`. Added migration runtime verification (`SHOW idle_session_timeout`) and persisted audit outcome (`fixed` / `fixed-verify-failed`, plus observed runtime value) so Update Now can confirm success without operator shell checks. **Part 2 (URL/routing):** hardened `_ensure_authentik_proxy_external_hosts_canonical` with retries + post-update re-run after Authentik API readiness. Added `ak shell` ORM fallback when API PATCH path cannot converge in-field. This closes tak-10 regression where Authentik providers remained on `https://taktical.test12.taktical.net` and canonical URLs (`nodered.*`, `takportal.*`) returned Authentik 404. Validated after Update Now: `Node-RED Proxy https://nodered.test12.taktical.net/`, `TAK Portal Proxy https://takportal.test12.taktical.net/`, cookie domains correct; browser URLs restored. Runtime validation snapshot: `idle_session_timeout=0`, `statement_timeout=2min`, `CONN_MAX_AGE=10`, restart counts stable (`0`), DB state healthy (`active=1`, `idle=32`), Authentik CPU low steady-state. See `docs/RELEASE-v0.9.22-alpha.md`.

- **v0.9.21 — Authentik PG connection leak fix + YAML/XML parse-and-mutate + drift self-healing + logo fix — RELEASED 2026-05-14, validated on tak-10 (test12.taktical.net, Authentik 2026.2.3).** Multi-session field investigation correctly identified root cause of `max_connections=500` exhaustion: NOT `django_channels_postgres` (initial hypothesis, upstream [#20714](https://github.com/goauthentik/authentik/issues/20714)) but the **enterprise license cache miss** pattern. Community Authentik never writes `public::1:goauthentik.io/enterprise/license` to `django_postgres_cache_cacheentry`, so every gunicorn/daphne worker generates a guaranteed DB miss on every request. Authentik's async-to-sync thread pool abandons threads without triggering Django's request-end cleanup hook, so `CONN_MAX_AGE` has no effect on those connections — only server-side expiry works. Key insight: `CONN_MAX_AGE` is the wrong lever; `idle_session_timeout` on Postgres is the right one. **Five-part fix:** (1) `idle_session_timeout=30s` re-added to pg_cmd (was 300s→removed→30s; 30s distributes expirations continuously with no burst CPU spike); `statement_timeout=120s` added. (2) `_ensure_authentik_compose_patches()` converted from regex text manipulation to PyYAML parse-and-mutate (eliminates duplicate `command:` keys reported in SAM operator handoff issues #1/#6). `CoreConfig.xml` LDAP auth converted from text to ElementTree parse-and-mutate (eliminates duplicate `<auth>`/`<ldap>` elements). (3) `_auto_remove_stale_docker_service_connections`, `_ensure_embedded_outpost_authentik_host` (new), `_ensure_authentik_proxy_external_hosts_canonical` (new) all wired into `_startup_migrations` for guaranteed reach. Closes tak-10's brand-subdomain drift (`external_host=https://taktical.test12.taktical.net` → canonical `https://nodered.test12.taktical.net`). (4) `CONN_MAX_AGE` default lowered 60→10; new `_patch_authentik_conn_max_age_60_to_10` startup migration for existing `=60` installs; PG connection watchdog retargeted to total idle connections (threshold 150, interval 120s). (5) Authentik logo URL fixed — `web/icons/icon_left_brand.png` (404 after GitHub repo reorganization) → `website/static/img/icon_left_brand_colour.svg` (pure `#fd4b2d` red, no white layer). **Validated on tak-10:** idle connection count stabilized at 14→60→14 over 3 minutes (no longer monotonically climbing to 500). Ref: [goauthentik/authentik#20714](https://github.com/goauthentik/authentik/issues/20714). See `docs/RELEASE-v0.9.21-alpha.md`.

- **v0.9.20 — MediaMTX HLS Caddyfile fix + Caddyfile auto-regenerate on Update Now + Authentik PostgreSQL pool tuning + Guard Dog 8089 false-positive fix — FULLY RELEASED 2026-05-13, all four parts validated on tak-10 (test12.taktical.net, MediaMTX v1.18.1).** Before/after on tak-10: Authentik server CPU 807%→29%, worker 73%→0.3%, PG connections 221→157, idle-in-tx 75→0, `channels_postgres_message` 109,840 rows / 61 MB → 3,834 rows / 40 MB (97% rowcount reduction), Guard Dog 8089 watch `exit=0`/`failcount=0`, zero new worker errors for 18+ min post-deploy.** (see `docs/RELEASE-v0.9.20-alpha.md`). Three fixes shipped together. **Part 3 — Authentik PostgreSQL pool tuning.** After v0.9.18 dropped the LDAP routing spiral on tak-10 from 492% CPU to ~5%, the box surfaced a different problem: Authentik server CPU climbing to 800%+, Postgres to 180%, **221 total Postgres backends** with **75 in `idle in transaction`** all pinned to the `django_postgres_cache_cacheentry` query, repeated `FATAL: sorry, too many clients already` in authentik-worker log, and `django_channels_postgres_message` bloated to **109,840 rows / 61 MB**. Same pattern reported upstream: [goauthentik/authentik#20714](https://github.com/goauthentik/authentik/issues/20714) (pool exhaustion) and [#20644](https://github.com/goauthentik/authentik/issues/20644) (channels_postgres growth + cleanup gaps). Root cause: Authentik 2026.2.x ships with Django's `CONN_MAX_AGE=0` default — every web request, every worker tick, every dramatiq job opens a brand-new Postgres connection (TCP handshake + Django startup queries) and tears it down on response. Stacked across server + worker + outpost flows, this saturates the 300-slot pool that infra-TAK set in v0.8.4. Slow cache table queries sit in `idle in transaction` waiting on the next round-trip — which arrives on a fresh connection because the old one was closed — and the pool fills. `django_channels_postgres` (Authentik's PG-backed realtime channel layer) compounds: under heavy outpost flow load it writes one row per outbound websocket message, TTL cleanup falls behind, table bloats to 100k+ rows, each LISTEN/NOTIFY query gets slower, idle-in-tx window lengthens, pool fills faster. **Three coordinated migrations:** **(a)** `_ensure_authentik_compose_patches()`'s `pg_cmd` template now uses `max_connections=500` (up from 300). Detection broadened: `max_connections<500` triggers full command-line rewrite via the same regex+rewrite path that already handles `idle_in_transaction_session_timeout != 300s`. Won't downgrade boxes where the operator set a higher value (smoke-tested: `=800` left alone). Triggers a one-time `docker compose up -d --force-recreate postgresql` to apply (postgres command-line args only take effect on container start). Same template change replicated in the deploy-from-template path at ~line 27734 for fresh installs. **`_authentik_fix_pg_idle_timeout()`** early-return broadened: only short-circuits when BOTH `idle_in_transaction_session_timeout=300s` AND `max_connections>=500` — otherwise calls `_ensure_authentik_compose_patches` + force-recreates Postgres, so boxes that already passed the v0.8.8 idle-timeout fix don't skip the v0.9.20 max_connections bump. **(b)** New helper `_ensure_authentik_pg_persistent_connections(plog, max_age=60, health_checks=True)` modelled on `_ensure_authentik_gunicorn_timeout`. Appends two env vars to `~/authentik/.env`: `AUTHENTIK_POSTGRESQL__CONN_MAX_AGE=60` (reuse connections for 60 s, ~95% handshake-churn reduction) and `AUTHENTIK_POSTGRESQL__CONN_HEALTH_CHECKS=true` (Django pings each pooled connection before reuse, drops dead ones — strictly required when CONN_MAX_AGE > 0 to avoid `connection already closed` on first request after a Postgres restart). Both keys use **double-underscore between segments** per [Authentik configuration docs](https://docs.goauthentik.io/install-config/configuration/) — same gotcha as the v0.8.2→v0.8.6 `AUTHENTIK_WEB_WORKERS` silent-ignore bug. Idempotent: if either key is already present (operator override / future Authentik default), it's left alone; only the missing keys are appended. After write, calls `_recreate_authentik_server_worker(reason='pg-persistent-connections')` to recreate server + worker only (LDAP outpost / Postgres untouched). Dramatiq workers also open Django DB connections, so leaving worker on `CONN_MAX_AGE=0` would leave half the churn problem in place. Verifies via `docker exec authentik-server-1 printenv AUTHENTIK_POSTGRESQL__CONN_MAX_AGE` (expect `60`) and `docker exec authentik-worker-1 printenv AUTHENTIK_POSTGRESQL__CONN_HEALTH_CHECKS` (expect `true`). Outcome saved to `settings.authentik_pg_persistent_conn_migration`. Wired into three call sites: post-Authentik-deploy (`_run_authentik_deploy_local` ~line 28800), post-TAK-deploy (~line 35247), and post-update hook (~line 39134) — all after the existing `_ensure_authentik_gunicorn_timeout` call. **(c)** `_detect_authentik_ldap_spiral()` evidence dict gains `channels_msg_rows` and `channels_msg_mb` from a `SELECT count(*) FROM django_channels_postgres_message` + `pg_total_relation_size('django_channels_postgres_message')/1024/1024` query. Plog surfaces values tiered: silent < 5k rows (healthy baseline), `elevated but not critical` 5k-49k, `⚠ bloat (cleanup task may be stuck — consider worker restart)` ≥50k. Does **not** trip the spiral gate (routing repair shouldn't fire on a bloat-only signal — table can be empty during a real spiral, see tak-10's first incident; a stuck cleanup task is an Authentik-side problem to mitigate at the operator level, not blanket-fix). Signal exists so operators can correlate symptoms during incident response without docker-execing into Postgres. **Smoke-tested before push** with 9 synthetic compose/.env cases (covering: v0.8.8-era box `300/300s` → bumped to `500/300s` cleanly, no duplicate `command:` lines; v0.9.20 box `500/300s` → no-op; v0.8.4-era `300/30s` → both fields upgraded together; operator-set `800` → not downgraded; fresh `.env` no `AUTHENTIK_POSTGRESQL__` vars → both appended; idempotent both-set → file unchanged; partial operator pre-set `CONN_MAX_AGE=120` → operator value preserved, only CONN_HEALTH_CHECKS appended; no trailing newline → first line preserved on its own). All 28 assertions pass. **Wiring-gap follow-up (same v0.9.20-alpha string).** During tak-10 validation after the initial push, max_connections=500 landed and CPU dropped 807%→17%, but `~/authentik/.env` was missing both `CONN_MAX_AGE` and `CONN_HEALTH_CHECKS`. Audit `authentik_pg_persistent_conn_migration: {}` (empty), no `.env.bak.pg-persistent-conn.*` backup, no `Post-update: pg persistent connections` log line — helper never ran. Root cause: tak-10 had pulled an earlier v0.9.20-alpha dev SHA (`ea0c951`, Caddy-only Caddyfile-auto-regen, no Authentik changes) BEFORE the full v0.9.20 commit (`2970470`) landed. The first pull's post-update hook set `last_console_version=0.9.20-alpha`. The second pull (full v0.9.20) had the same VERSION string, so the post-update hook `if last_ver == VERSION: return` short-circuited with no banner and no migration thread. `max_connections=500` only landed because `_authentik_fix_pg_idle_timeout` is ALSO wired into the **startup-migration block** (line 39123 of `_startup_migrations`) which is NOT version-gated and runs on every console restart — it caught the `max_connections<500` drift, force-recreated Postgres, wrote `last_outcome: fixed` audit at `2026-05-14T00:24:43Z`. But `_ensure_authentik_pg_persistent_connections` and `_ensure_authentik_gunicorn_timeout` were ONLY in the version-gated post-update hook + the full-Authentik-deploy path + the TAK-deploy path. None fired. **Fix:** added both helpers to the startup-migration block right after `_authentik_fix_pg_idle_timeout`, with the same `try/except` + `Startup migration:` log prefix. Idempotent — both helpers no-op when their env vars are already set, so safe to call unconditionally on every console restart. Discovered the gap for `_ensure_authentik_gunicorn_timeout` too in passing: its audit `ts: 1777481628` decodes to `2026-04-29` (Apr 29), meaning the migration ran ONCE long ago (back when console ran as root with different `~/authentik` resolution) and has been a no-op since because the post-update hook on `last_ver == VERSION` boxes never re-fired it. Now both will reconcile on every boot. **No VERSION bump for this fix** because the whole point is that the startup-migration block runs on plain `systemctl restart takwerx-console`, not on version change. Boxes already at `last_console_version=0.9.20-alpha` pick up the helpers on the next Update Now's restart cycle without needing a version-change trigger. Bumping VERSION specifically because the burn-VERSION-then-add-migrations pattern caused this exact problem would compound the same pattern. Lesson for future releases: **migrations that need guaranteed reach should live in the startup-migration block** (`_startup_migrations` around line ~38131-39160 in app.py), not in the version-gated `_post_update_auto_deploy`/`_run_post_update` thread. Post-update should be reserved for genuinely one-shot heavy operations (reactive routing repair, big DB migrations) where re-running on every boot would be wasteful or risky. **Two fixes from the v0.9.19 dev iteration also ship here:** (1) **Caddyfile template** — `generate_caddyfile()`'s MediaMTX block `handle_path /hls-proxy/*` now emits a single `header_down Location ^ /hls-proxy` directive inside the `reverse_proxy` block. MediaMTX v1.18.0 introduced HLS session tracking: first manifest request returns `302 → ?cookieCheck=1` with a `Set-Cookie: mediamtx_session=...` header. infra-TAK strips `/hls-proxy/` via `handle_path` before forwarding to `127.0.0.1:8888`; MediaMTX's redirect `Location` is relative to its own path (`/teststream/index.m3u8?cookieCheck=1`), so the browser follows it to `/teststream/...` on `stream.<base>` which has no Caddy handler → 404 → HLS Watch broken on every infra-TAK server running MediaMTX v1.18.0+. `header_down Location ^ /hls-proxy` is a regex replacement on the upstream response's `Location` header (`^` start anchor, `/hls-proxy` prepended), so MediaMTX's redirect becomes `/hls-proxy/teststream/...?cookieCheck=1` before reaching the browser. On `200 OK` (manifest served), no `Location` header → `header_down` is a no-op. Safe across MediaMTX v1.17.0 (no cookie check issued, dead code) and v1.18.0+ (fixes the redirect). Reference doc: [INFRA-TAK-CADDY-HLS-v1.18.md](https://github.com/takwerx/mediamtx-installer/blob/main/docs/INFRA-TAK-CADDY-HLS-v1.18.md). **Earlier draft used `handle_response @mtx_redirect { redir ... }` (commit `a275cc3`) — DOES NOT WORK** because the `stream.<base>` block has a catch-all `route {}` with `forward_auth` for the MediaMTX web editor, and when `handle_response` issues `redir` the route chain doesn't terminate cleanly: `forward_auth` still fires on `/hls-proxy/*` and the browser gets redirected to Authentik authorize instead of the manifest. `header_down` modifies the upstream response in-place without generating a new response, so route chain terminates and `forward_auth` never runs on `/hls-proxy/*`. The mediamtx-installer doc was revised same-day with the corrected approach (revision 2, 2026-05-13). (2) **Update Now never rewrote `/etc/caddy/Caddyfile`** — surfaced during the v0.9.19 HLS rollout: Caddyfile template change in `generate_caddyfile()` was correct, but Update Now (`fetch → checkout → systemctl restart takwerx-console`) does not call `generate_caddyfile()` afterwards. Template code on disk was updated, rendered Caddyfile on disk was stale, live Caddy kept serving the old config. Added a final step to `_post_update_auto_deploy()`'s `_run_post_update()` (right before `print("Post-update: auto-deploy complete")`) that loads settings, and if `fqdn` is set and `/etc/caddy/Caddyfile` exists, calls `generate_caddyfile(settings)` + `systemctl reload caddy` (20s timeout). Gated by the existing version-change check (`if last_ver == VERSION: return`) so it fires once per version bump, idempotent no-op when FQDN isn't set / Caddy isn't installed. From v0.9.19 forward, any Caddyfile template change ships via Update Now alone — no operator click on the Caddy page required. Note: this version-change-gated approach left dev-channel boxes that pulled v0.9.19 dev commits sequentially before the hook landed stuck at `last_console_version=0.9.19-alpha` with the version-change gate short-circuiting on the next dev pull — which is why v0.9.20 is cut as a fresh version rather than continued as v0.9.19 iteration. The version bump `0.9.19 → 0.9.20` fires the hook on every box including the dev fleet, so the Caddyfile auto-regenerate step runs cleanly on the next Update Now without operator workaround. Main-channel boxes coming from `v0.9.18-alpha` were always going to get the bump cleanly via the v0.9.18→v0.9.20 version change. **Part 4 — Guard Dog 8089 false-positive restart fix ([issue #24](https://github.com/takwerx/infra-TAK/issues/24)).** `scripts/guarddog/tak-8089-watch.sh` previously did two checks per minute: (1) `ss -ltn "sport = :8089"` LISTEN probe, (2) `nc -z -w 5 127.0.0.1 8089` raw TCP-connect probe. Port 8089 is configured with `auth="x509" protocol="tls"` (mutual TLS) — `nc -z` completes the TCP 3-way handshake but sends no TLS ClientHello, Netty immediately closes the connection on the TLS error, and on some netcat-openbsd builds the RST/FIN arrives before nc has reported success → non-zero exit → `CONNECT_OK=false` → `LISTEN_OK && CONNECT_OK` fails → false-positive unhealthy increment every minute → 3 restarts hit the daily cap → every subsequent check logs `SKIP | 8089 unhealthy but daily restart cap (3) reached` in the Guard Dog activity log. TAK Server is healthy; the health check is broken. Fix: removed the `nc -z` block + `CONNECT_TIMEOUT` constant + `CONNECT_OK` variable. Healthy condition is now just `LISTEN_OK` (port is bound per `ss -ltn`) — sufficient because if Netty is bound, the messaging process is accepting connections. Detail log line updated from `listen=$LISTEN_OK connect=$CONNECT_OK` to `listen=$LISTEN_OK`. No logic changes to restart thresholds, cooldown, daily cap, or alerting. Same `nc -z 127.0.0.1 8089` → `ss -ltn "sport = :8089" | grep -q LISTEN` change applied to `scripts/guarddog/tak-post-start.sh`'s boot-wait loop, which had been polluting the TAK server log with SSL errors on every boot. The `nc -z 127.0.0.1 389` LDAP probes elsewhere in `tak-post-start.sh` are intentionally KEPT — port 389 is plaintext LDAP (no TLS), so `nc -z` works correctly there. Scripts are auto-deployed to `/opt/tak-guarddog/` on every console startup via `_auto_update_guarddog()` (diffs repo scripts against deployed copies, copies any that changed) — the `Guard Dog: 2 script(s) updated on console startup.` log line at v0.9.20 startup confirmed both scripts auto-deployed without operator "Deploy Guard Dog" click. Validated on tak-10: deployed scripts confirmed to have new code (grep for live `nc -z 8089` / `CONNECT_OK` / `CONNECT_TIMEOUT` returns 0 matches), live run returns `exit=0`, `/var/lib/takguard/8089.failcount` stays at `0`. **Part 5 — Wiring-gap fix validation (no VERSION bump).** The wiring-gap fix described in Part 3 was specifically validated by **NOT bumping VERSION** on the wiring-gap fix push and confirming the new migration helpers ran via `_startup_migrations` on next `systemctl restart takwerx-console`. tak-10 was the same box where the gap was discovered. After the fix landed, the next `Update Now` correctly applied the missing env-vars on a box that previously had `last_console_version=0.9.20-alpha` and was being silently skipped by the version-gated post-update hook. This validates the architectural decision: **migrations that need guaranteed reach should live in `_startup_migrations`, not in version-gated `_post_update_auto_deploy`**. Both call sites are kept (defense in depth) but the startup-migration block is now authoritative. **Lesson for the next dev**: when adding a new self-healing migration, wire it into BOTH `_startup_migrations` (unconditional, every console restart, for guaranteed reach) AND a fresh-deploy code path (so brand-new installs converge to target state on first boot). Skip `_post_update_auto_deploy` unless the migration is genuinely one-shot heavy and re-running on every boot would be wasteful or risky (reactive routing repair, big DB migrations).
- **v0.9.18 — Authentik LDAP routing repair hotfix — RELEASED 2026-05-13, validated on tak-10** (see `docs/RELEASE-v0.9.18-alpha.md`). Critical fix for the v0.8.4 reactive (`_apply_authentik_ldap_routing_repair`) and v0.8.5 proactive (`_ensure_authentik_ldap_outpost_on_fqdn`) LDAP outpost routing migrations. Both functions rewrote `~/authentik/docker-compose.yml`'s `ldap:` service block in a single pass: for each line, if it was the `image:` line and `ldap_has_extra_hosts` was still `False`, inject a new `extra_hosts:` block right after `image:`. **Bug:** `image:` comes BEFORE `extra_hosts:` in the file, so when the LDAP block already had an `extra_hosts:` (e.g. from an earlier partial run), the rewriter hadn't seen it yet when processing `image:` — it injected a SECOND `extra_hosts:` key, then passed the ORIGINAL `extra_hosts:` block through unchanged. Result: duplicate mapping keys → `failed to parse /root/authentik/docker-compose.yml: yaml: construct errors: line 107: mapping key "extra_hosts" already defined at line 105` → `docker compose up` refused → rewrite rolled back → spiral monitor retried 10 min later → forever loop. Discovered on `tak-10` (test12.taktical.net): 492% Authentik CPU, 74 `failed to execute flow`/min, ~213 PostgreSQL connections from authentik-server, six failed repair attempts in 70 min between 21:10 and 21:51. **Fix:** new shared helper `_rewrite_ldap_compose_to_fqdn(compose_text, fqdn) → (new_text, image_seen)` that pre-scans the LDAP block in one pass to detect (a) block boundaries via the same 2-space-key heuristic as before, (b) whether `extra_hosts:` already exists, (c) whether the LDAP image line exists at all — then runs the rewrite pass only touching lines inside the LDAP block. Skips `extra_hosts:` injection when already present (the existing block is kept verbatim, including custom operator entries). Always rewrites `AUTHENTIK_HOST:` to `https://<fqdn>`. Returns `image_seen=False` for callers to abort when the LDAP service is missing/corrupt. Both call sites refactored to use the helper (removes ~50 lines of duplicated code from `_apply_authentik_ldap_routing_repair` and `_ensure_authentik_ldap_outpost_on_fqdn`). Validated against 5 real-world compose states: (1) tak-10 stuck state — extra_hosts present + internal AUTHENTIK_HOST → 1 extra_hosts entry, AUTHENTIK_HOST rewritten, valid YAML; (2) clean compose (no extra_hosts, internal AUTHENTIK_HOST) → 1 extra_hosts entry added + AUTHENTIK_HOST rewritten; (3) no LDAP service → `image_seen=False`, no changes; (4) LDAP block with non-LDAP image → `image_seen=False`, no changes; (5) already on FQDN → idempotent. Also confirmed the OLD code produces two `extra_hosts:` keys on case 1 (the exact tak-10 bug). Risk: very low — the helper only runs on boxes the spiral monitor or proactive migration has already gated as eligible (TAK installed, FQDN configured, Caddy reachable, currently on internal direct routing). Idempotent everywhere else.
- **v0.9.17 — Authentik Redis self-heal + Caddy update restart fix — RELEASED 2026-05-13, validated on three boxes** (see `docs/RELEASE-v0.9.17-alpha.md`). Two fixes found during v0.9.16 follow-up fleet check. (1) `_ensure_authentik_compose_patches()` now detects and injects a missing `redis:` service into existing `docker-compose.yml` files. Installs deployed before Redis was part of the Authentik compose template had no Redis container — Authentik's dramatiq worker retried `localhost:6379` on every task cycle causing 40–65% sustained CPU (`responder` server at 64.93%). Root cause: the fresh-deploy path always included Redis, but `_ensure_authentik_compose_patches()` (the patcher for existing installs) only patched PG tuning and healthchecks, never Redis. Three idempotent patch steps added: (a) Redis service block injected before `server:` if `redis-cli ping` not in file; (b) `AUTHENTIK_REDIS__HOST: redis` added to every environment block containing `AUTHENTIK_POSTGRESQL__HOST:` if key absent from file; (c) `redis:` added to top-level `volumes:` section if missing. All three are no-ops on installs that already have Redis. After patching, the existing `docker compose up -d` in `_auto_authentik()` starts the new Redis container and recreates server/worker (env changed). Validated: responder dropped from 64.93% → 0.39% on Update Now; ssdnodes (already had Redis) stayed at 0.25% (no-op). (2) `caddy_update()` now runs `systemctl restart caddy` (timeout 90s) instead of `systemctl reload caddy || systemctl restart caddy`. After an apt binary upgrade, `reload` is semantically wrong (keeps old binary, only re-reads config) and can hang indefinitely mid-ACME-challenge — systemd repeats the timeout every ~90s until accumulated failures cause it to SIGTERM/SIGKILL the main Caddy process (issue #25, observed on tak.test6.takwerx.com: reload loop 17:49→18:41). Restart is unconditionally correct post-upgrade.
- **v0.9.16 — Authentik worker CPU hotfix + Caddy update button + Authentik update spinner — RELEASED 2026-05-13, validated on two boxes** (see `docs/RELEASE-v0.9.16-alpha.md`). Three changes: (1) `_auto_remove_stale_docker_service_connections()` added to `_run_post_update()`. Deletes the "Local Docker" service connection that Authentik's upstream quickstart creates by default. The v0.9.2 CVE-2026-31431 hardening removed `/var/run/docker.sock` from the worker compose but left the service connection in Authentik's DB — worker's `outpost_service_connection_monitor` task retried the dead socket every 30s causing ~26% sustained CPU. Fix uses Authentik API `GET /api/v3/outposts/service_connections/docker/` + `DELETE` for each `local: true` connection. Idempotent, non-fatal. Timing: runs after `_auto_authentik()` health-check loop to ensure Authentik API is responsive. **Post-release finding (fixed in v0.9.17):** the migration only helps the `local Docker service connection` case; installs missing Redis entirely (deployed before Redis was added to the template) had a completely different CPU root cause unaddressed by v0.9.16. (2) Caddy detail page now shows current installed version + `update available` cyan indicator in the status banner (fed from the existing `_get_caddy_version_info()` apt check). New `⬆ Update` button in controls runs `apt-get install --only-upgrade caddy` then `systemctl reload caddy` via new `POST /api/caddy/update` route. **Post-release finding (fixed in v0.9.17):** `reload` hangs mid-ACME-challenge; changed to `restart` in v0.9.17. Consistent with update button pattern on all other service pages (TAK Portal, CloudTAK, FedHub, Guard Dog, MediaMTX). (3) Authentik `⬆ Update` button now calls dedicated `akUpdate()` JS function (was `akControl('update')`). Shows spinner + status line during `docker compose pull` (2–5 min); green/red result on completion. Matches Caddy and TAK Portal UX. **JS lesson:** `\n` in Python template strings renders as a literal newline — embed as `\\n` to produce the two-character JS escape sequence in rendered output; literal newlines inside JS single-quoted strings cause `SyntaxError: Invalid or unexpected token`.
- **v0.9.15 — TAK Portal admin-account guardrail — SHIPPED 2026-05-12** (see `docs/RELEASE-v0.9.15-alpha.md`). Drop-in defense-in-depth release on top of v0.9.13 + v0.9.14. v0.9.13's incident class (TAK Portal user clicks **Disable** on `akadmin` and `webadmin`, locks operator out of Authentik) is now closed from three independent directions: **Prevent** (v0.9.15 — TAK Portal hides + action-locks both protected admins via `USERS_HIDDEN_PREFIXES` + `USERS_ACTIONS_HIDDEN_PREFIXES` defaults; operator can't click Disable / Delete on them); **Detect** (v0.9.13 + v0.9.14 — Protected Admin Accounts panel on `/authentik` shows live `is_active` state, reads survive Authentik 403s via ak-shell fallback); **Recover** (v0.9.13 + v0.9.14 — one-click Reactivate, layered API → `ak shell` recovery). TAK Portal author (Justin Davis) surfaced that TAK Portal already had the two settings fields needed for this; infra-TAK is already authoritative for both in `_takportal_build_settings_dict` so the change is two strings: `USERS_HIDDEN_PREFIXES` `"ak-,adm_,nodered-,ma-"` → `"akadmin,webadmin,ak-,adm_,nodered-,ma-"` and `USERS_ACTIONS_HIDDEN_PREFIXES` `""` → `"akadmin,webadmin"`. (TAK Portal does PREFIX matching; the existing `ak-` had a trailing dash and did not cover the literal `akadmin`.) New self-healing migration `_auto_harden_takportal_settings()` runs in `_run_post_update` right after `_auto_harden_takportal` (port hardening): reads live `settings.json` from the `tak-portal` container; if both prefixes are already in both fields, no-op; otherwise pushes merged settings (preserving `BRAND_LOGO_URL`, `TAK_SSH_ONBOARDED`, `TAK_SSH_LAST_HANDSHAKE_AT` via `PRESERVE_TAKPORTAL_KEYS`) and restarts the container. Skipped when TAK Portal isn't deployed locally — remote-Portal installs apply the new defaults via TAK Portal → Update config & reconnect in the console UI. Recovery surface unchanged from v0.9.14. **Design rule for the next dev:** when a recovery feature exists for a foot-gun, the recovery panel should be the last layer of defense, not the first. Prevention (hide the foot gun) > Detection (show it's been pulled) > Recovery (unbreak it). v0.9.13/14 built layers 2+3 first because we had an incident; v0.9.15 closes layer 1. All three are kept in place because any of them can fail (operator clears the prefix lists; future TAK Portal release exposes a way past the lock; Authentik's native admin UI is still always usable for `is_active` toggles).
- **v0.9.14 — Authentik Admin Recovery hotfix — SHIPPED 2026-05-12** (see `docs/RELEASE-v0.9.14-alpha.md`). Drop-in hotfix to v0.9.13. v0.9.13 shipped the **Protected Admin Accounts** panel but the *status read* path (`_get_authentik_admin_accounts_status`) only knew how to talk to the Authentik REST API — when `akadmin.is_active = false` (the exact state the panel exists to recover from), the `AUTHENTIK_BOOTSTRAP_TOKEN` (owned by `akadmin`) authenticates but Authentik returns **HTTP 403** on every protected endpoint because the token's *owner* has no permissions. Net effect on customer installs that had already hit the bug: panel rendered `? Authentik API 403` for both accounts, never reached the `is_active === false` branch, never drew the **Reactivate** button — the recovery panel couldn't see the very state it was supposed to expose. v0.9.13 had built the right architecture for the *write* path (`_recover_authentik_user`: API → `docker exec authentik-server-1 ak shell` fallback) but missed the symmetric problem on the *read* path. v0.9.14 introduces `_read_authentik_admin_accounts_via_ak_shell()` (reads both whitelisted users in one `docker exec` via a base64-encoded Django ORM snippet, parses `AK-STATUS|<user>|EXISTS|<is_active>|<is_superuser>` lines) and wires it into `_get_authentik_admin_accounts_status()` as a layered read: API first, fall back to ak-shell for *all* users if any API call errors so the panel renders from one consistent source. Response JSON gains `source: 'api'|'ak-shell'` for diagnostics; UI shows a dim caption `status read via ak shell (Authentik API unavailable)` when the fallback is active. UI escape hatch: the JS now also renders the Reactivate button when `a.error` is set (not only when `a.is_active === false`) so the operator always has a manual lever even if both read paths fail — the recover endpoint has its own independent ak-shell fallback that doesn't depend on the read working. **Lesson recorded for the next dev**: a recovery feature has to survive the failure mode it's recovering from. If you can express the failure as "the bootstrap token works *iff* akadmin is active," then any read **or** write through the bootstrap token is a bootstrap problem and needs the same ak-shell escape hatch. v0.9.13 nailed the write path and missed the read path; do not repeat.
- **v0.9.13 — Authentik Admin Recovery — SHIPPED 2026-05-12** (see `docs/RELEASE-v0.9.13-alpha.md`). Operator unlock-out recovery feature, prompted by a real incident where a TAK Portal user clicked **Disable** on both `webadmin` and `akadmin`. Authentik's "Disable" = `is_active=false` (reversible, just blocks login + LDAP bind), but with both protected admins disabled the operator is locked out of the Authentik UI entirely. New **Protected Admin Accounts** panel on `/authentik` (under the existing "Admin user: akadmin · Show Password" row) polls `is_active`/`is_superuser` for both accounts and shows a red `⚠ DEACTIVATED` row with a **Reactivate** button when an account is down. `_recover_authentik_user()` recovery is layered: (1) `PATCH /api/v3/core/users/{pk}/` with `AUTHENTIK_BOOTSTRAP_TOKEN` from `~/authentik/.env` (normal case → banner `[via api]`); (2) `docker exec authentik-server-1 sh -c 'echo $b64 | base64 -d | ak shell'` running Django ORM `User.objects.filter(username=…).save()` — base64-encoded so no quoting concerns across shell/ssh/docker layers; bypasses API auth, broken flows, broken policies (→ banner `[via ak-shell]`). Whitelist `_AUTHENTIK_RECOVERABLE_USERS = ('akadmin', 'webadmin')` enforced server-side so the endpoint cannot be used to re-enable arbitrary accounts, and the whitelist also guarantees the username interpolated into the ak-shell snippet is one of two literals. Bundled one-line fix: `_ensure_authentik_webadmin()` now also includes `is_active: True` in its patch fields, matching what `_ensure_authentik_ldap_service_account` already did for `adm_ldapservice` — without this the existing **Sync webadmin to Authentik** button silently failed to recover a disabled webadmin (it would set the password but `is_active=false` blocks the LDAP bind, so 8446 login still failed). No migrations, no operator pre-flight; drop-in update from v0.9.12. Late-cycle UI bug shipped in the same release: the initial `btnHtml` build for the Reactivate button used `\'` escapes inside a Python triple-quoted JS string — Python's triple-quoted string parser interprets `\'` as `'` even inside `'''...'''`, so the rendered JS string terminated mid-expression and the whole `<script>` block failed to parse with `Uncaught SyntaxError: Unexpected string`, leaving `refreshAdminAccounts` undefined and the Refresh button silently dead. Fix: switch that line to a JS **template literal** (backticks) — Python doesn't interpret backticks, and single quotes inside backticks don't need escaping. Recorded in the release roadmap so the next dev-template author who reaches for `\'` in embedded JS knows to use template literals instead.
- **v0.9.12 — Cyber Security Hardening — SHIPPED 2026-05-11** (see `docs/RELEASE-v0.9.12-alpha.md`). Generalised the v0.9.11 CloudTAK `!reset` override pattern to TAK Portal, MediaMTX (local + remote), remote Authentik, and CloudTAK's other public-bound services (`api`/`tiles`/`events`/`media`); added new `_auto_harden_takportal()`, `_auto_harden_mediamtx()`, `_auto_authentik_ports_remote()` post-update steps; patched the post-auth code bugs surfaced by the 2026-05-10 audit (snapshot path traversal via new `_validate_snapshot_label`, external-DB SQLi via psql `-v` substitution + identifier regex, webadmin-password RCE via argv-only `subprocess.run`, external-DB test-connection RCE via `socket.create_connection` replacing `bash -c "</dev/tcp/..."`, hardcoded `adm_ldapservice` fallback password replaced with `secrets.token_urlsafe(24)` persisted to `.env`, SSH host/user injection via new `_validate_ssh_target`). Also locked down Server One Postgres + Guard Dog health-agent UFW (removed unconditional `allow {port}/tcp` that overrode source-scope rules above). Ships `docs/PORT-EXPOSURE-POLICY.md` as the canonical Tier 1/3/4/5 reference. **Late-cycle additions during test phase:** B7 HOME pinning fix in `takwerx-console.service` (operator clicked Sync webadmin and `cd ~/authentik` failed under gunicorn — same class of bug as v0.2.7's `takupdatesguard.service`, finally closed for the console unit); B8 Authentik ReputationPolicy `negate=True` (binding semantics were inverted — `passes()` returns `True` for *bad* reputation, so `negate=False` denied all normal users; hid for 10 releases because LDAP outpost's `bind_mode: cached` masked the misconfig). Self-healing startup migrations now patch existing v0.9.11 installs end-to-end on first Update Now (port hardening compose files, LDAP SA bind drift, ReputationPolicy binding drift).
- **v1.0.0 — NEXT MAJOR. Non-root console migration** (planned, see `docs/PLAN-v1.0.0.md`). Moves the console off `User=root` to a `takwerx` sudo user. Reserved for v1.0.0 because the runtime behavioural change warrants the major-version semver bump. Scaffolding (`_sudo_wrap`, `_write_priv`, `_read_priv`) already in place.
- **v0.9.14 — Auth hardening + UI cleanup** (tentative, may slot in before or after v1.0.0). Shared-secret header for `X-Authentik-Username` trust, `SESSION_COOKIE_SECURE`, masked secrets in API responses with re-auth-to-reveal, `atakatak` keystore rotation button.

## Tooling discipline

- **Cursor rules in `.cursor/rules/`:** `consult-upstream-docs.mdc` (always-applied) — read upstream docs before chasing symptoms. `cursor-memory-bank.mdc` — this memory bank pattern.
- **Top-level `.cursorrules`:** Node-RED safety contracts (FEEDS empty, never raw-`docker cp`, deploy.sh safety gate, never wipe global context).
- **Pre-release maintainer process:** `docs/TESTING-UPDATES.md` — fake-low VERSION, click Update Now, watch the migration ladder, restore. Pushing the tag without testing is forbidden.
- **Selective merge** `dev` → `main` for releases — `docs/COMMANDS.md`.

## Container hardening — known cap_drop exclusions

Several containers legitimately need their default capability set and must NOT have `cap_drop: ALL` applied:

- **CloudTAK `api`** — runs both nginx and Node.js in the same image. `cap_drop: ALL` silently breaks nginx workers (can't write `/dev/stdout`) and the nginx→Node loopback proxy. No error in logs — workers just die with exit 2.
- **TAK Portal** — Node.js reads `tak-client.p12` (owned by uid 889, mode 600). `cap_drop: ALL` removes `CAP_DAC_OVERRIDE`; the read silently fails and the dashboard shows `--` for all stats.
- **Authentik worker** — similar issue; `cap_drop: ALL` broke the worker in a way that produced no obvious log errors.

Node-RED is the exception — it legitimately runs with `cap_drop: [ALL]` and `no-new-privileges:true` (it doesn't need elevated capabilities).

The rule: before adding `cap_drop` to any service, verify the container doesn't mix multiple processes (nginx + app) or need filesystem access across UID boundaries.

## CloudTAK Reset Config notes

`cloudtak_reset_server_config` uses `TRUNCATE profile CASCADE` (not `DELETE WHERE system_admin=true`). Reason: `profile_overlays`, `profile_config`, and other tables have FK references to `profile.username`. A plain DELETE hits FK violations. TRUNCATE CASCADE handles all dependent tables. This is correct for a reset — all profile data is tied to the old TAK Server's user accounts. The error check uses exit code only (not string scan for "ERROR") because `TRUNCATE CASCADE` emits `NOTICE:` messages to stderr that would false-positive on string checks.

## Fail2ban / Scheduler toggle pattern

The `*-toggle-track` spans must NOT have `onclick` — they are already inside a `<label>` that natively toggles the checkbox on click. Adding `onclick=".click()"` causes double-fire (onchange fires twice per click), making disable impossible. As of v0.9.5, all 7 track spans have no onclick.

## Authentik Postgres — two-cluster setup and known maintenance issues

There are **two completely separate Postgres clusters** on every infra-TAK install with Authentik:

1. **TAK Server Postgres** — runs on the host as the `postgres` OS user, holds the `cot` database. Guard Dog auto-vacuum and the POSTGRES-DIAGNOSTICS.md runbook target this cluster. Generally healthy.

2. **Authentik Postgres** — runs inside `authentik-postgresql-1` Docker container (UID 70, `postgres:16-alpine`). Separate DB, separate process, separate config. All Authentik psql commands must go through `docker exec authentik-postgresql-1 psql -U authentik`.

**Known recurring issue — task log bloat:** `authentik_tasks_task` and `authentik_tasks_tasklog` grow unbounded (~500–900 MB after 1 month). The `takauthentiktasklogpurge.timer` (v0.9.5+) handles weekly cleanup. `_authentik_tasklog_cleanup()` (v0.9.6+) also runs the DELETE + VACUUM on "Update Now" if either table exceeds 100 MB — clears the one-time backlog on first update.

**`shm_size: 256m` required on `postgresql` service** — Docker's 64 MB default `/dev/shm` is too small for `VACUUM ANALYZE` with parallel workers in postgres:16-alpine. Multiple bugs across v0.9.5/v0.9.6/v0.9.7:
- v0.9.5: regex to add shm_size matched wrong field order (looked for `restart` then `command`, but compose has them reversed); also `--force-recreate` never included `postgresql`
- v0.9.6: whole-file `'shm_size:' not in file` check false-positives when server/worker services have their own `shm_size` values; docker inspect check only ran when `'shm_size: 256m' in file`
- v0.9.7 (fixed): anchor detection on postgres image line, scan only the postgresql service block; docker inspect check is unconditional — always compares `HostConfig.ShmSize` against `268435456`
- v0.9.7 (new bug): `docker compose up -d --force-recreate` default 10s stop timeout too short for loaded postgres — process survives container stop as orphan at 1100%+ CPU
- v0.9.8 (fixed): `docker stop -t 30` gives postgres 30s to checkpoint; cgroup-based orphan check runs unconditionally on every update — reads `/proc/<pid>/cgroup` for all UID-70 postgres processes, kills any not belonging to current container ID
- v0.9.8 (new bug, verified on responder): orphan check runs at end of `_auto_harden_containers()`, but `_auto_authentik()` runs LATER and recreates containers again — fresh orphans were not caught by the first check
- v0.9.9 (fixed): second cgroup-based orphan kill right before `auto-deploy complete`, after `_auto_authentik()` finishes its reconfigure-time recreate
- v0.9.8/v0.9.9 (new bug, friendly fire): the cgroup check used `kill if cgroup does not contain authentik-postgresql-1 ID` — this also killed UID-70 postgres processes inside `cloudtak-postgis-1` (CloudTAK's PostGIS spatial DB), which restart-loop on every update and spike CPU to 800–1100% catching up. Verified on responder and tak-10
- v0.9.10 (fixed): check against ALL running Docker container IDs (`docker ps -q --no-trunc`), kill only when cgroup matches NO running container; preserves legitimate processes in any postgres container regardless of name

**Authentik vs CloudTAK postgres** — both run as UID 70 on the host. The cgroup is the only reliable way to tell them apart at the process level. Never assume "UID 70 + not in container X" means orphan; always check against the full set of running containers.

**v0.9.11 — CloudTAK upstream RCE / PG_MEM cryptominer (security hotfix)** — dfpc-coe/CloudTAK ships `postgis ports: 5433:5432` on `0.0.0.0` with hardcoded `POSTGRES_PASSWORD=docker` literal. Live compromise observed on responder May 8-10 2026: scanner → brute-force `docker:docker` succeeds → `COPY FROM PROGRAM` drops `gcmanager-1.so` into postgis data volume → modify `postgresql.conf` `shared_preload_libraries` → Monero miner runs at 1000%+ CPU with C2 over Tor SOCKS5. Family: PG_MEM/PGMiner (Aqua Nautilus, Palo Alto Unit 42). Sample hash on responder: SHA256 `715348a40250549100cbbeb2a8d68ffa323e671b55fc46e8df24c7016b11e10a` (566 KB ELF static-pie, statically-linked musl libc, compiled for Alpine). Hashes vary across deployments — reliable IOC is the persistence technique (.so in data dir + shared_preload_libraries in postgresql.conf), not the hash.

**v0.9.11 mitigation in app.py:**
- `_cloudtak_build_override_yml()`: `postgis ports: !reset []` removes the public 5433 mapping entirely (CloudTAK uses internal Docker network); `store ports: !reset` binds only `127.0.0.1:9002:9002` (drops public 9000 S3 API, keeps console on loopback for SSH tunnel); `postgis environment.POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:-docker}"` substitutes from .env.
- `_cloudtak_build_env_content()`: new `postgres_pass` parameter, emits `POSTGRES_PASSWORD=` line + uses it in `POSTGRES=postgres://docker:<pass>@postgis:5432/gis` connection string. Default 'docker' for backwards compat.
- 4 caller sites updated: fresh-install paths (2) generate `secrets.token_hex(24)` and save to `~/CloudTAK/.postgres-password` (chmod 600); reconfig paths (2) read existing value from `.env` (local) or via SSH grep from remote `.env` to preserve DB connection.
- New `_auto_harden_cloudtak()` runs every Update Now after `cloudtak_t.join()`: scans postgis data volume for `*.so` files + uncommented `shared_preload_libraries` in `postgresql.conf`. If compromised: stops all CloudTAK containers, quarantines `.so` files to `quarantine-YYYYMMDD-HHMMSS/` subdir (preserves forensics, does not delete), comments out the malicious config line with `#INFRATAK_DISABLED# ` prefix, writes `~/CloudTAK/COMPROMISE-DETECTED.txt`, prints loud banner, leaves CloudTAK STOPPED pending operator Remove + Reinstall. Always (idempotent): writes hardened override, applies UFW deny rules for 5433/9000/9002, force-recreates only if clean.
- Critical: Postgres reads `POSTGRES_PASSWORD` env var ONLY on `initdb` (first boot of empty volume). On existing volumes the env var is ignored and the previously-baked password persists. This is why update-only flows can't rotate the password on existing installs — only Remove + Reinstall (which wipes the volume) gets a fresh strong password baked in.

**Operator paths post-v0.9.11:**
- Update Now alone = network locked down via override + UFW; existing weak password remains but unreachable; compromised installs quarantined + stopped.
- Update Now + Remove + Reinstall (recommended) = wipes data volume + reinstall path generates strong password baked into fresh DB. Full remediation.

**Authentik 2026.x task table schema** — `authentik_tasks_task` PK is `message_id` (uuid), timestamp is `mtime`. The old assumed column names (`pk`, `finish_timestamp`) do not exist. `authentik_tasks_tasklog.task_id` → `authentik_tasks_task(message_id)`. Correct DELETE: `WHERE message_id IN (SELECT message_id ... WHERE mtime < NOW() - INTERVAL '30 days')`.

## v0.9.12 lessons learned (May 2026)

Patterns and gotchas that emerged during v0.9.12 testing on `tak-10` and `responder`. Each section is durable — if you see the symptom, the fix is here.

### Pattern: systemd unit must pin `Environment=HOME=` if the service shells out with `~`

**Symptom (v0.9.12):** clicking **Sync webadmin to Authentik** returned `/bin/sh: 1: cd: can't cd to ~/authentik` even though `/root/authentik` clearly existed. `_ensure_authentik_ldap_service_account` and `_auto_harden_*` paths could fail intermittently with the same error. Surfaces specifically under gunicorn-spawned `subprocess.run('cd ~/… && …', shell=True)` calls.

**Root cause:** systemd does **not** inherit `HOME` from login env. `takwerx-console.service` only pinned `Environment=PYTHONUNBUFFERED=1` and `Environment=CONFIG_DIR=…`. Under gunicorn, `os.environ.get('HOME')` returned `None`, and `/bin/sh` cannot expand `~` without `$HOME`. Same root cause as:
- **v0.2.7-alpha** — `takupdatesguard.service` needed `Environment=HOME=…` for the Guard Dog Updates timer.
- **v0.9.2-alpha** — `git safe.directory` / `git config --global` writes failed because `git` couldn't find `~/.gitconfig`.

**Three-layer fix pattern (canonical for this class of bug):**
1. **Runtime guard at the top of `app.py` (after stdlib imports):** if `os.environ.get('HOME')` is unset, derive from `pwd.getpwuid(os.getuid()).pw_dir` (fallback `/root`) and write it to `os.environ`. Fixes the **current** process immediately; child shells inherit.
2. **Installer (`start.sh create_service()`):** new installs get `Environment=HOME=$SERVICE_HOME` baked into the unit file (`$SERVICE_HOME` derived from `${HOME:-/root}`).
3. **Idempotent startup migration (`_startup_pin_console_service_home()`):** patches existing v0.9.11- unit files on Update Now (sed-in-place insert of `Environment=HOME=/root` after the existing `Environment=` block, `systemctl daemon-reload` only when a change was made).

**Rule of thumb:** any future systemd unit added under `/etc/systemd/system/` that wraps a Python/bash process which might `subprocess.run('cd ~/…', shell=True)` **must carry `Environment=HOME=…`**. If you're adding a new unit, set it at create time. If you find a unit missing it in the field, add a migration like `_startup_pin_console_service_home()`.

### Pattern: Authentik PolicyBinding fields require DELETE+POST to change (PATCH returns 405)

**Symptom (v0.9.12):** trying to fix the `negate` / `failure_result` fields on an existing `infratak-brute-force` PolicyBinding via `PATCH /api/v3/policies/bindings/{pk}/` returned `405 Method Not Allowed` on Authentik 2026.2.2.

**Workaround:** DELETE the existing binding, then POST a fresh one with the corrected fields. Used in both `_authentik_setup_reputation_policy()` retro-fix branch and `_startup_fix_reputation_policy_drift()`. Capture the existing binding's `pk` from a GET first, DELETE by `pk`, then POST the new body — order matters because POST will fail with a uniqueness error if the old binding still exists on the same (policy, target) pair.

### Pattern: Authentik `ReputationPolicy.passes()` semantics are inverted from intuition

**The bug, verified against Authentik 2026.2.2 source via `inspect.getsource(ReputationPolicy.passes)` on the running container:**

```python
def passes(self, request: PolicyRequest) -> PolicyResult:
    # ... aggregate Reputation score for this IP/username ...
    passing = score <= self.threshold
    return PolicyResult(bool(passing))
```

`passes()` returns `True` **only when the user has accumulated enough bad reputation to act on** (score ≤ threshold, threshold is negative — default `-5`). A normal user with `score = 0` returns `False`. Therefore:

| Binding `negate` | Normal user (score 0) | Brute-force abuser (score ≤ -5) | Result |
|---|---|---|---|
| `False` (intuitive default) | binding denies | binding allows | **EVERYONE denied** (intuitive but wrong) |
| `True` (correct) | binding allows | binding denies | only abusers blocked (correct brute-force gate) |

**Cardinal rule (now documented in `docs/HANDOFF-LDAP-AUTHENTIK.md`):** when adding a ReputationPolicy as a brute-force gate on `ldap-authentication-flow`, ALWAYS set `negate=True, failure_result=True` on the PolicyBinding.

**Why this hid for ten releases (v0.9.2 → v0.9.12-rc):** the LDAP outpost runs in `bind_mode: cached`. Once `adm_ldapservice`'s first successful bind landed in the cache, all subsequent binds for that DN were served from cache without re-consulting the flow. The misconfigured binding never had a chance to fail in production. v0.9.12's `_startup_resync_ldap_service_account` force-recreates the LDAP outpost on every console boot (to fix a separate password drift issue), wiping the bind cache and **exposing** the long-standing reputation-policy misconfig. This is a generalisable warning: any cached upstream behaviour can mask config bugs for a long time; wiping the cache is what surfaces them. Test cache-clearing paths in pre-release.

### Pattern: never raw-copy YAML edits via Python regex backreferences when the value contains regex metacharacters

**Symptom (v0.9.12 pre-rc):** `_patch_takportal_compose_ports` and `_patch_cloudtak_compose_ports` corrupted `docker-compose.yml` files into `ports:J7.0.0.1...` (literal `J` inserted) and similar garbage. Operator-reported as "TAK Portal map not working, weird YAML errors."

**Root cause:** `re.sub(r'(ports:\s*)\N\1...', ...)` with `\N` backreferences silently consumes characters when the captured group contains characters that look like backreferences. Python's `re` module treats `\1` ... `\9` as backreferences in the replacement string, and `\N` (capital N) is ambiguous.

**Fix:** use `\g<N>` syntax instead of `\N` for ALL Python regex backreferences in replacement strings. This is unambiguous. Also add corruption detection: if a compose file no longer parses as YAML after a patch, `git checkout` it from the current branch and re-apply.

**Generalised rule:** when you find yourself doing `re.sub` on a structured config file (YAML, JSON, ini), strongly prefer:
1. Parse the file with the proper parser (`yaml.safe_load`), modify the data structure, serialise back. This is the right answer 90% of the time and avoids all regex pitfalls.
2. If you MUST use regex (because the file has comments / formatting / `!reset` directives you need to preserve), always:
   - Use `\g<N>` not `\N` for backreferences.
   - Validate the file parses correctly after the substitution.
   - Have a fallback: detect corruption, `git checkout` from `origin/$branch`, re-apply.

### Pattern: Docker Compose `ports: !reset` is NOT compatible with `docker-compose.yml` if the base file has `ports: [...]` as a list (vs map)

Discovered while generalising the v0.9.11 CloudTAK pattern to TAK Portal and CloudTAK's other services. The `!reset` tag works when overriding compose files **at the override layer**, but if the base `docker-compose.yml` already has `ports:` as an explicit list, dropping `!reset` into that file directly is a syntax error in some compose versions.

**Generalised rule:** the `!reset []` pattern in `_cloudtak_build_override_yml()` belongs in the **override file** (`docker-compose.override.yml`). For the base compose files (`docker-compose.yml`), patch the port mappings **directly** by parsing the file and rewriting the `ports:` list to bind only loopback. Don't try to inject `!reset` into the base file. The corrected v0.9.12 implementation uses `_patch_takportal_compose_ports` and `_patch_cloudtak_compose_ports` which parse + rewrite + serialise.

### Pattern: end-to-end auto-heal migrations need to verify with the authoritative source (not log inspection)

**Bug we briefly shipped in v0.9.12-rc:** `_test_ldap_bind()` checked Authentik server logs for `Bind successful` markers. This gave **false positives** because positive log lines from previous successful binds (cached) were within the `--since` window even when the current bind attempt was failing.

**Fix:** replaced with `_test_ldap_bind_dn_verdict(dn, password)` which runs `ldapsearch -x -H ldap://… -D <dn> -w <pw> -b 'dc=takldap' -s base '(objectClass=*)'` and captures the **ldapsearch exit code directly into a variable** (don't rely on `$?` after pipelines). Returns one of `'ok' | 'fail' | 'inconclusive'`. Authoritative because it actually attempts the bind through the live LDAP outpost; doesn't depend on any log parsing.

**Generalised rule:** when self-healing migrations need to know whether a state has been reached, prefer the **authoritative live probe** (CLI exit code, API GET, direct DB query) over **log scraping**. Logs are append-only and a stale positive will mask a current negative. The only exceptions are when:
1. The log line is being written by the **current** process whose `pid` you can match, OR
2. You can `truncate -s 0` / rotate the log immediately before the action.

### Pattern: Authentik blueprint plaintext password fallback can drift `adm_ldapservice` even when the operator never touched it

**The recurring class of bug** (this has bitten infra-TAK multiple times — May 2026 v0.9.12 was the most recent):

The Authentik blueprint applied by `_authentik_apply_blueprint()` historically contained `password: !Env [AUTHENTIK_BOOTSTRAP_LDAPSERVICE_PASSWORD]` inside the service-account user definition. When the blueprint engine processes this on every Authentik startup, it writes the env var value to the user's `password` column **as plaintext**, not as a hash. Authentik then refuses to authenticate against the stored password because all bind paths expect the hashed form.

**Symptom:** `ldapsearch -D 'cn=adm_ldapservice,ou=users,dc=takldap' …` returns 49 (Invalid Credentials) right after a fresh blueprint apply (e.g. Update Now), even though `~/authentik/.env` has the expected `AUTHENTIK_BOOTSTRAP_LDAPSERVICE_PASSWORD=<value>` and that value should be the one Authentik is checking.

**Why it stays hidden:** the LDAP outpost's `bind_mode: cached` serves previously-successful binds from cache. The plaintext-vs-hash mismatch only manifests on the **first** bind after a cache clear (container recreate, outpost restart, fresh deploy). Once you hit it manually with `set_password` via the Authentik API (which always writes a proper hash), subsequent binds succeed and the cache rebuilds; the operator never sees the bug for weeks.

**v0.9.12 mitigation (`_startup_resync_ldap_service_account`):**
1. Test the bind with `_test_ldap_bind_dn_verdict` (authoritative).
2. If it fails, call `POST /api/v3/core/users/{pk}/set_password/` with the `.env` value — Authentik hashes it correctly.
3. Recreate the LDAP outpost container to clear its cache.
4. Retest. If success, restart `takserver` so it picks up the new bind (TAK Server caches LDAP credentials itself; restart is the only documented way to flush that cache).
5. Persist outcome to `settings.json` so future boots can audit what happened.

**Generalised rule:** when an Authentik blueprint contains a literal `password:` field, ALWAYS audit it for `!Env` substitution that would write plaintext. Either remove the password from the blueprint entirely (set it via API afterwards) or accept that you need a startup-time resync migration to put the correct hash in place after every blueprint apply.

### Pattern: when verifying upstream behaviour, read the live container source, not docs

`.cursor/rules/consult-upstream-docs.mdc` is the always-applied rule that says read official docs first. The v0.9.12 ReputationPolicy bug surfaced the **next** level of rigour: even the docs can be ambiguous or omit edge cases. The decisive proof on `tak-10` was:

```python
docker exec authentik-server-1 ak shell -c "
import inspect
from authentik.policies.reputation.models import ReputationPolicy
print(inspect.getsource(ReputationPolicy.passes))
"
```

That ran the live code path against the deployed image (`authentik:2026.2.2`), printed the exact `passing = score <= self.threshold` line, and proved `negate=True` was required. **Generalised rule:** for any "the API says one thing but my install behaves differently" symptom, drop into the running container's interpreter and use `inspect.getsource()` to dump the live function. Authentik exposes `ak shell` which is a Django shell with all the auth models pre-imported — perfect for this.

## Things that are NOT in scope (yet)

- Air-gapped install.
- Non-root console runtime (scaffolded, deferred).
- Windows / RHEL host support.
- Kubernetes deployment.
- Multi-tenant console (one VPS = one customer).
- TAK Server clustering beyond the official `cluster` flag in CoreConfig.
